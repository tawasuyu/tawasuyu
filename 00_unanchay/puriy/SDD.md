# Puriy — navegador web soberano

> Puriy (quechua: *viajar, recorrer, caminar*). Tipo: **Web browser engine + chrome over Llimphi**.

## Tesis

Motor web propio en Rust, soberano, con render delegado a [[Llimphi]]. Resultado: un navegador que corre idéntico en:

- **Linux/Wayland** — Llimphi monta sobre `mirada` (compositor).
- **Wawa bare-metal** — Llimphi monta sobre framebuffer directo (sin OS).

Una sola pila gráfica (`wgpu + vello + taffy + DAG monádico`), una sola superficie abstracta (`llimphi-hal::Surface`), dos targets.

### Historia: de Servo a arquitectura propia

La tesis original era **embebar Servo entero** (`script + style + layout + net`) y sustituir webrender por Llimphi. En la decisión del 2026-05-26 (Opción A pragmática) optamos por usar **sólo crates puntuales de upstream** — `html5ever` y `markup5ever_rcdom` para parsear HTML, `cssparser` como anchor; el resto (style engine, cascade, layout, render, networking) es **propio**. A medida que el motor maduró (selectores compound + combinadores + atributos + pseudoclases estructurales + nth-child + not + hover + focus + cascada con especificidad + !important + var() + calc() + pseudo-elements ::before/::after), quedó claro que NO vamos a "adoptar Stylo entero" — la pila propia ya está más alineada con Llimphi y con wawa que cualquier port de Servo. Servo quedó como inspiración inicial, no como dependencia arquitectónica.

## Por qué motor propio en Rust

- **Rust nativo** — sin FFI a C++. Tipos seguros, sin segfaults heredados.
- **Sin runtime corporativo** — no Chromium, no WebKit, no Stylo upstream.
- **Compatible con wawa** — el motor no asume threadpool, ni libc, ni allocator. Selectores y cascada compilan en `no_std` (la separación está en progreso).
- **Bridge directo a Llimphi** — el box tree pasa a `llimphi-raster` sin display lists intermedios.
- **Crates upstream usados** — sólo parsers (`html5ever 0.39`, `markup5ever_rcdom 0.39`, `cssparser 0.35` como anchor) + `ureq` para net + `url` para resolución. El resto es propio.

## Anatomía — 4 crates

```
[ CUADRANTE I · 0x00 UNANCHAY ]

4. puriy-app          — Binario lanzable (en mirada o en wawa)
   │                    (parsea CLI, instancia engine, abre Llimphi)
   ▼
3. puriy-llimphi      — Chrome del navegador
   │                    (toolbar, tabs, address bar, bookmarks)
   │                    Construido sobre llimphi-ui (DAG monádico)
   ▼
2. puriy-engine       — Motor propio en Rust
   │                    (parser HTML via html5ever; style+cascade+layout+net propio)
   │                    Output: BoxTree → llimphi-raster vía puriy-llimphi
   ▼
1. puriy-core         — Modelo agnóstico
   │                    (sesiones, tabs, history, bookmarks, perfiles)
   │                    Sin deps de Servo ni de Llimphi
   ▼
[ Estado puro ]
```

## Fases de forja

### Fase 1 — `puriy-core` (modelo agnóstico)

- `Session`, `Tab`, `History`, `Bookmark`, `Profile` puros.
- Sin deps de gráficos ni de Servo.
- Testeable con `cargo test`.
- **Hito:** abrir un Profile en disco, crear/cerrar tabs, navegar (mock).

### Fase 2 — `puriy-engine` (embed de Servo)

- Agregar deps de los crates Servo necesarios (no todo Servo, solo `script`, `style`, `layout`, `net`, `webrender_api` quizá).
- Bridge entre `puriy-core::Tab` y la pipeline Servo.
- **Decisión arquitectónica clave:** ¿usamos `webrender` interno de Servo o forzamos toda primitiva a pasar por `llimphi-raster`?
  - **Opción A (pragmática):** `webrender` para el viewport del documento, Llimphi para el chrome. Servo se mantiene cerca de upstream.
  - **Opción B (purista):** Interceptar el `Display List` de Servo y traducirlo a primitivas Vello dentro de `llimphi-raster`. Más trabajo, soberanía total.
  - **Decisión:** Empezar con A en Fase 2; migrar a B cuando Llimphi madure y haya un caso de uso real (ej: renderizar páginas en wawa sin pulling webrender entero).
- **Hito:** Cargar `https://example.com` y renderizar el DOM parseado en una textura wgpu.

### Fase 3 — `puriy-llimphi` (chrome)

- Toolbar (back/fwd/reload/url) + tabs + sidebar opcional.
- Construido sobre `llimphi-ui` (DAG monádico).
- Eventos de teclado: Ctrl+T (nuevo tab), Ctrl+W (cerrar), Ctrl+L (focus address bar), etc.
- **Hito:** Chrome funcional sin engine (engine devuelve mocks).

### Fase 4 — `puriy-app` (binario)

- CLI: `puriy [URL] [--profile NAME] [--target wayland|framebuffer]`.
- Detección automática del target (si hay variable `WAYLAND_DISPLAY` → mirada; si no, framebuffer wawa).
- **Hito:** `puriy https://gioser.net` abre y renderiza la landing del propio repo.

## Pila exacta

| Capa | Crate raíz | Deps externas |
|---|---|---|
| Core | `puriy-core` | (puro Rust) |
| Engine | `puriy-engine` | `servo` (selección de crates), `tokio`, `url` |
| Chrome | `puriy-llimphi` | `llimphi-ui`, `llimphi-layout`, `llimphi-raster` |
| App | `puriy-app` | todo lo anterior + `clap` |

## Targets de salida (vía `llimphi-hal::Surface`)

| Target | Surface impl | Cuándo |
|---|---|---|
| Wayland (dev / desktop normal) | `WinitSurface` sobre `mirada-compositor` | Linux con sesión gráfica |
| Framebuffer bare-metal | `WawaFramebufferSurface` (impl en `03_ukupacha/wawa/`) | Cuando `wawa` es PID 1 y no hay OS host |
| Headless (tests / CI) | `HeadlessSurface` (sin display) | `cargo test`, screenshots |

## Estado

- **2026-05-25:** SDD escrito. Esqueletos de los 4 crates creados (sin deps de Servo todavía).
- **2026-05-26:** Fase 2 — `puriy-engine` real. Deps de Servo embebidos: `html5ever 0.39` (parser HTML), `markup5ever_rcdom 0.39` (DOM), `cssparser 0.35` (anchor; el subset CSS se parsea con un mini-parser propio porque la API de cssparser rotó entre 0.33→0.35 y nuestro subset es trivial), `url 2`. Net síncrono con `ureq` (no tokio en el engine). Pipeline `fetch → parse_html → parse_styles → build_box_tree → BoxTree` operativo: `cargo run -p puriy-app -- https://example.com` baja la página, parsea DOM + UA stylesheet + `<style>` inline + atributo `style="..."`, y dumpea el árbol de boxes. 10/10 tests verde. **Decisión arquitectónica:** se eligió Opción A (pragmática) — webrender se mantiene fuera por ahora, el box tree pasa directo a `llimphi-raster`. Opción B (interceptar Display List Servo→Vello) se reconsidera cuando el motor adopte Stylo entero.
- **2026-05-26:** Fase 3 — `puriy-llimphi` real. `App` Llimphi (`Puriy`) con header (URL + status) + viewport blanco. Worker thread carga la URL; el `BoxTree` cruza al UI thread por `Handle::dispatch` (el `DomTree` con `Rc<Node>` queda en el worker y se dropea ahí — es `!Send`). Conversión recursiva `BoxNode → View<Msg>`: blocks columnan, inlines fluyen en row, colores y spacing mapean a `Style` de taffy. F5 recarga. `puriy-app` autodetecta target: `WAYLAND_DISPLAY`/`DISPLAY` → ventana Llimphi; sino → headless. **Probar:** `cargo run -p puriy-app -- https://example.com` (abre ventana). `cargo run -p puriy-app -- https://example.com --target headless` (dumpea árbol).
- **2026-05-26 (tarde):** Fase 3 polish. (1) **Scroll vertical**: wheel + PageUp/Dn + ArrowUp/Dn + Home/End, mediante `Position::Absolute` + `inset.top = -scroll_y` + `clip(true)` en el outer. (2) **Links clickables**: `BoxNode.link` resuelve `<a href>` contra base URL (engine), el chrome dispara `Msg::Navigate(url)` y recolorea el subárbol del `<a>` en azul. (3) **Address bar editable**: `TextInputState` (single-line del editor compartido) sobre `text_input_view`; click foca el input, Enter navega, Esc cancela. (4) **Polish**: UA stylesheet con `font_weight` (h1..h6/b/strong/th = 700), parser CSS para `font-weight`, `<li>` prefija bullet, `pre/blockquote/hr` añadidos a block defaults. Bold se simula con `font_size × 1.1` mientras `llimphi-text` no exponga el eje weight.
- **2026-05-26 (noche):** Features de chrome. (a) **Historial por pestaña**: `TabState.history: Vec<String>` + `cursor`. Navigate trunca y empuja; Back/Forward recargan sin push. Botones ◀ ▶ ⟳ + atajos Alt+←/→. (b) **Pestañas múltiples**: `Model.tabs: Vec<TabState>` + `active`. Barra superior con click-para-activar y "✕" para cerrar; "+" abre nueva. Atajos Ctrl+T / Ctrl+W / Ctrl+Tab / Ctrl+Shift+Tab. Cada Msg async lleva `tab: TabId, gen: u64` — si la pestaña fue cerrada o pisada por otra navegación, el resultado se descarta. (c) **`<img>` mínimo**: engine descarga + decodifica (`image` crate, PNG+JPEG) sync dentro del worker; `BoxNode.image: Option<ImageData>` (RGBA8 + w/h). Chrome envuelve en `peniko::Image` y lo aplica con `.image(img)`. Fallback `[img: alt]` si la decodificación falla.
- **2026-05-26 (madrugada):** Fix amontonado. Hojas de texto reciben `height = font_size × 1.4` (line-height aproximado) — sin esto, taffy colapsaba los inlines al top del bloque. Bloques con sólo hijos inline/inline-block conmutan a `FlexDirection::Row + FlexWrap::Wrap` para que los tokens fluyan en múltiples líneas. Bloques con hijos block siguen en `Column` sin wrap.
- **2026-05-26 (siguiente):** Fidelidad de render. (a) **Whitespace inline**: `collapse_whitespace` en `NodeData::Text` reemplaza el `trim` duro — colapsa runs internos a un espacio, preserva uno leading/trailing si existía. Resuelve casos `foo <a>bar</a> baz` que antes salía como `foobarbaz` pegado. Whitespace-only nodes entre inlines se conservan como `" "` para no perder la separación. (b) **Selectores `.class` y `#id`**: `Selector` enum (`Universal | Type | Id | Class`); el parser parsea selectores simples con prefijo `.` o `#` (alfanuméricos + `-_`); combinadores/pseudoclases/atributos se ignoran en silencio. UA stylesheet migrado al enum. Tests `selector_class_matchea` y `selector_id_matchea`.
- **2026-05-26 (continuación):** Network + CSS. (a) **Cache de bytes**: módulo `puriy_engine::cache` con `HashMap<String,Vec<u8>>` global protegido por `Mutex`, LRU por orden de inserción, cap 64 MB con eviction. `fetch_bytes` y `fetch_and_decode` consultan cache antes de salir a la red — recargas (F5), back/forward y navegación entre tabs del mismo origen son instantáneas. (b) **Selectores descendientes**: `Selector` ahora es compound (`parts: Vec<Simple>`); `Simple` mantiene `Universal | Type | Id | Class`. El matcher recorre ancestros greedy de derecha a izquierda usando `node.parent` de `markup5ever_rcdom`. Acepta `.menu li`, `nav a`, `#hero h2`, etc. Pseudoclases y combinadores `> + ~` siguen ignorándose. Test `selector_descendiente_matchea` (15/15 verde).
- **2026-05-26 (sgte):** Selectores CSS — combinadores y compounds. (a) **Simples compound**: el viejo `enum Simple` se aplastó en `struct Compound { tag, ids, classes }` — un único compound puede mezclar tag + N ids + N classes (`a.btn`, `p#hero.alert`, `*.foo`). El parser barre el token con un cursor de bytes en vez de `chars().all(is_ident_char)`. (b) **Combinadores `> + ~`**: `Selector` ahora lleva `compounds: Vec<Compound>` + `combinators: Vec<Combinator>` (descendant/child/adjacent-sibling/general-sibling). `normalize_combinators` inyecta espacios alrededor de `>`/`+`/`~` antes de tokenizar. El matcher viaja derecha→izquierda y por cada combinador salta a parent o a `prev_element_sibling` (que saltea text nodes). Sin pseudoclases ni `[attr]` todavía — la regla se ignora si aparecen. Tests: `selector_compound_matchea`, `selector_hijo_directo_matchea`, `selector_hermano_adyacente_matchea`, `selector_hermano_general_matchea` (19/19 verde).
- **2026-05-26 (sgte+1):** Selectores CSS — atributos y pseudoclases estructurales. `Compound` se infló con `attrs: Vec<AttrMatch>` y `pseudos: Vec<Pseudo>`. (a) **Atributos**: `[attr]` (presencia), `[attr=v]` (igual), `[attr^=v]` (prefijo), `[attr$=v]` (sufijo), `[attr*=v]` (substring). `parse_attr_match` busca operadores en orden `^=`/`$=`/`*=`/`=` y trimea comillas. (b) **Pseudoclases estructurales**: `:first-child`, `:last-child`, `:only-child`, `:first-of-type`, `:last-of-type`. `pseudo_matches` filtra hijos Element del padre (saltea Text nodes) y resuelve por posición / tipo. `:hover`/`:focus`/`:active` quedan fuera porque requieren tracking del chrome; `:not(...)` y `:nth-child(...)` también: cualquier paréntesis tras `:` rechaza el selector entero. (c) **`normalize_combinators`** ahora respeta lo que vive dentro de `[…]` o `(…)` — `[href*="a>b"]` no se rompe. Tests nuevos: `selector_attr_presente`, `selector_attr_equals`, `selector_attr_prefix_suffix_contains`, `selector_first_last_only_child`, `selector_first_last_of_type` (24/24 verde).
- **2026-05-26 (sgte+2):** Propiedades CSS de layout. `ComputedStyle` gana `width`/`max_width: LengthVal` (`Auto`/`Px`/`Pct`), `text_align: TextAlign` (`Left`/`Center`/`Right`/`Justify`) y `line_height: Option<f32>` (multiplicador). `parse_length_or_pct` acepta `auto`, `Npx`, `Nem`/`Nrem`, `N%`; `parse_line_height` traga `1.5` (mult adimensional), `24px`, `1.5em`. `BoxNode` propaga los cuatro campos; `puriy-llimphi::box_style` mapea `width`/`max_width` a `Size`/`max_size` de taffy (`Pct(80)` → `percent(0.8)`), `text-align` de un bloque con hijos inline a `justify_content` del Row (`Center`/`End`), y `line-height` reemplaza el `1.4` hardcodeado en la altura de hojas de texto. Tests nuevos: `parsea_width_max_width`, `parsea_text_align`, `parsea_line_height`, `computa_width_y_text_align` (28/28 verde).
- **2026-05-26 (sgte+3):** Inheritance CSS real. **Bug original**: cada hoja de texto se construía con defaults hardcoded (`BLACK`, `16px`, `400`), aunque su `<p>` padre dijera `color:red; font-size:20px`. **Fix**: `StyleEngine::compute_with_parent(node, parent)` arranca el `ComputedStyle` copiando del padre las propiedades CSS-inheritables (`color`, `font_size`, `font_weight`, `text_align`, `line_height`) y deja en default las no-heredables (`background`, `display`, `margin`, `padding`, `width`, `max_width`). `boxes::build_node` threadea el `parent_style` recursivamente; las hojas Text se construyen con `inline_text_with_style(s, parent_style)`. Refinamiento: `font_weight` por tag (`<b>`/`<strong>`/`h1..h6`/`<th>` = 700) sólo se aplica si el tag pinta bold; si no, se respeta lo heredado — así un `<b>` dentro de un `<p>` no-bold sigue siendo bold sin pisar herencia desde adentro hacia afuera. Tests nuevos: `hereda_color_y_font_size_del_padre`, `no_hereda_propiedades_no_heredables`, `font_weight_bold_local_no_propaga_a_padre_no_bold`, `box_tree_propaga_color_a_hoja_de_texto` (32/32 verde).
- **2026-05-26 (sgte+4):** Especificidad CSS real. **Bug original**: cascada por "último gana" — `#hero{color:blue}` perdía contra `body p{color:red}` si llegaba antes en el stylesheet. **Fix**: `Selector::specificity()` calcula el clásico `a*100 + b*10 + c` (a = #id, b = .class + [attr] + :pseudo-class, c = tags). En `compute_with_parent`, las reglas que matchean se recolectan con `(specificity, source_index, &Rule)` y se ordenan ASC antes de aplicarse — empate de especificidad lo desempata el orden de aparición. Inline `style="..."` sigue al final con especificidad implícita 1000. Tests: `specificity_calculada_correctamente` (incluye `a.btn[href^="https"]:first-child` = 31, `nav > a#x.y` = 112), `id_vence_a_tag_aunque_llegue_antes`, `clase_vence_a_tag`, `inline_style_vence_a_id`, `empate_de_especificidad_gana_el_ultimo` (37/37 verde).
- **2026-05-27:** `!important`. `Decl` ahora es `struct Decl { kind: DeclKind, important: bool }` — cada declaración puede ser marcada individualmente. El parser detecta el sufijo `!important` (case-insensitive) al final de cada value antes de pasar el resto a `decl_kind_from_pair`. La cascada hace dos pasadas en orden: (1) normales ordenadas por (specificity, source_index) más inline normal, (2) `!important` con mismo orden más inline `!important`. Cualquier important vence cualquier normal del mismo origen, lo cual abre la puerta a que stylesheets de autor "fuerzan" overrides. Tests: `important_vence_normal_de_mayor_especificidad`, `important_inline_vence_important_de_id`, `normal_inline_pierde_contra_important_de_regla` (40/40 verde).
- **2026-05-27 (sgte):** `border` + `border-radius`. (a) **Engine**: `ComputedStyle` gana `border_width: f32`, `border_color: Option<Color>`, `border_radius: f32`. `DeclKind` agrega `BorderWidth`, `BorderColor`, `BorderEnabled(bool)`, `BorderRadius`. `border-style: solid|dashed|dotted|double` activa el dibujo; `none|hidden` lo desactiva (color → None, width → 0). El shorthand `border: 2px solid #f00` se expande en `parse_declarations` a 3 decls atómicas vía `parse_border_shorthand` — los tokens pueden venir en cualquier orden (cada uno se prueba contra parse_length_px / parse_color / parse_border_style). (b) **Chrome**: `apply_border(view, &BoxNode)` aplica `View::radius(border_radius)` + `paint_with(...)` con `vello::Scene::stroke` sobre un `RoundedRect` insetado por `width/2` para que el trazo caiga dentro del rect (vello pinta centrado al path). Tests: `parsea_border_shorthand` (incluye `!important` y `border: none`), `parsea_border_propiedades_individuales` (42/42 verde).
- **2026-05-27 (sgte+1):** `:hover` con scope limitado. `Pseudo::Hover` se evalúa contra un flag externo `hover_active: bool` que el caller threadea (`compute_with_parent_in_state` lo expone). En `boxes::build_node` cada Element computa dos veces — sin hover (estilo base) y con hover_active=true; el delta sólo se captura en `hover_background: Option<Color>` por ahora. El chrome lo plug-ea con `View::hover_fill(...)` que ya tenía Llimphi para transiciones de bg. **Limitaciones explícitas**: (a) sólo se propaga el delta de background — color/border/font-weight no cambian on hover; restyle completo requeriría re-mount del view tree por evento de mouse. (b) `:hover` sólo aplica al sujeto del selector (último compound); `nav:hover a` no propaga el chain de hover a los ancestros. Suficiente para 90% del uso real (`.btn:hover`, `a:hover`). Tests: `hover_state_activa_regla_solo_cuando_corresponde`, `hover_pseudo_aporta_a_specificity`, `box_tree_expone_hover_background` (45/45 verde).
- **2026-05-27 (sgte+2):** Persistencia de cache entre sesiones. `cache::load_from_disk()` y `cache::flush()` serializan el bytes-cache a `$XDG_CACHE_HOME/puriy/cache.bin` (fallback `$HOME/.cache/puriy/cache.bin`). Formato binario manual sin deps externas: magic `PUYC` + versión u8 + count u32 LE + por entrada `[u32 url_len][url][u32 data_len][data]`. Escritura atómica vía `cache.bin.tmp` + rename. `puriy-app::main` llama `load_from_disk` al startup y `flush` al cierre; el worker del chrome también flush después de cada navegación exitosa para no perder todo si el proceso muere. Best-effort: errores I/O son silenciosos (perder el flush no rompe la sesión, sólo significa cold start la próxima). Tests: `codec_round_trip`, `decode_rechaza_magic_invalida` (47/47 verde).
- **2026-05-27 (cierre fase 4):** **HITO ALCANZADO** — `puriy https://gioser.net --target headless` parsea la landing del propio repo (título correcto, 455 boxes, jerarquía completa: header/main/aside/ul/li, footer, SVG). Cableado de `puriy-core` que faltaba: (a) **`--profile NAME` funcional**: `load_or_create_profile` resuelve `$XDG_CONFIG_HOME/puriy/profiles/NAME/` (fallback `~/.config/...`), carga `profile.json` con `puriy_core::store::load` o crea fresh con `Profile::nuevo`. Cada profile tiene su propio `cache.bin` (vía `cache::set_persist_path`). (b) **History global persistente**: en `run_headless` y en el chrome (`Msg::Loaded`), cada navegación exitosa llama `profile.history.record(url, title, now)` y `persist_all`/`persist_profile` guarda el JSON atómicamente. (c) **Bookmark con Ctrl+D**: nueva `Msg::Bookmark` que toma la URL+título de la pestaña activa, llama `profile.bookmarks.add` si no estaba (dedup por url), actualiza el status bar (`⭐ guardado · N bookmarks` / `⭐ ya estaba guardado`), y persiste. (d) **Tests E2E**: `puriy --target headless --profile demo` corrido dos veces preserva `history.len == 2` con ambas URLs (gioser.net + example.com). 62 tests verde (47 engine + 15 core).
- **Estado Fase 4:** CERRADA. Lo que queda es para fases siguientes: font-weight axis real (depende de llimphi-text), fetch async paralelo de assets (refactor del worker), `:hover` restyle completo (color/border), `:focus`/`:active`, JS engine via Servo `script` crate (Fase 5+).
- **Fase 5.1 (2026-05-27):** Feature polish CSS. (a) **`box-shadow`** — `ComputedStyle.box_shadow: Option<BoxShadow { offset_x, offset_y, blur_px, spread_px, color }>`. Parser acepta `2px 4px [blur] [spread] <color>` y `none`. En el chrome, `apply_decorations` (renombra de `apply_border`) pinta sombra + border en una sola closure de `paint_with`: vello `Fill::NonZero` sobre `RoundedRect` expandido por `blur+spread`, alpha ajustado según blur (aproximación sin gaussian blur multi-pass). (b) **`:nth-child(an+b)` y `:not(simple)`** — `Pseudo` gana `NthChild { a, b }` y `Not(Box<Compound>)`. Parser detecta `:name(args)` y consume hasta `)`. `parse_nth_arg` acepta `odd`/`even`/`N`/`an`/`an+b`/`an-b`/`-n+b`. `:not(...)` rechaza recursión y combinadores en el arg. Specificity: `:not(X)` aporta la especificidad de X (CSS spec). (c) **Cache TTL con `Cache-Control`** — entries ahora son `(bytes, expires_at: u64)`. `fetch_bytes` lee `Cache-Control` post-response, parsea `max-age=N`, computa `expires_at = now + N` y llama `put_with_expiry`. Headers con `no-store`/`no-cache` se cachean sin TTL (que es como sin cache header — equivalente conservador). `cache::get` evicta lazy: si la entrada expiró, la borra y devuelve `None` (miss limpio para refetch). Formato persistente bumpea a V2 con `u64 expires_at` por entrada; archivos V1 se ignoran al cargar. 58/58 verde (10 tests nuevos).
- **Fase 5.1 (sgte):** `text-decoration` — underline / line-through / overline. (a) **Engine**: `ComputedStyle.text_decoration: TextDecorationLine` (None/Underline/LineThrough/Overline). Parser de shorthand acepta `text-decoration: underline dotted red` y captura sólo el line (estilos/color se ignoran — pintamos línea sólida del color del texto). `text-decoration-line` es alias. (b) **Herencia**: CSS spec dice que NO se hereda en computed values (los descendientes inline pintan la línea por "propagación visual" del padre block). Acá la tratamos como heredable porque dibujamos por leaf de texto — sin propagar, `<a>foo <b>bar</b></a>` rendería `foo` subrayado pero `bar` no. Override explícito a `none` la suprime, lo cual diverge del spec pero matchea la intuición del autor (`a { text-decoration: none }` quita el underline al link entero). (c) **UA stylesheet**: `a`/`u`/`ins` → underline; `s`/`strike`/`del` → line-through. (d) **Chrome**: `apply_decorations` toma una tupla `(line_kind, color, font_size)` cuando el nodo es leaf con `text_decoration != None`. Dentro de la closure de `paint_with` se calcula la y relativa al rect (0.10 overline, 0.55 line-through, 0.88 underline — sin baseline real porque la hoja de texto tiene `height = font_size × line_height ≈ 1.4`, y el glifo vive arriba-centro), grosor `max(1.0, font_size × 0.07)` y `scene.stroke` sobre `kurbo::Line` del color del texto. 63/63 verde (5 tests nuevos).
- **Fase 7.5c (2026-05-29):** **Mutación reactiva del DOM — `el.textContent = X` repinta.** Tercer sub-paso de Fase 7.5: scripts ahora pueden cambiar el contenido visible. Combinado con 7.5a (timers) y 7.5b (handlers), cubre el patrón "clock que se actualiza solo", "contador que se incrementa al click", "label que cambia tras un fetch".
  - **Setters JS sobre el snapshot DOM**: `__puriy_make_element(id, tag, text)` ahora define `textContent` e `innerHTML` con `Object.defineProperty` — get devuelve `_textContent`, set lo actualiza y publica `{id, kind:'text', value}` a `globalThis.__puriy_dirty`. `innerHTML` se trata como `textContent` por ahora (sin parsear markup interno — suficiente para `el.innerHTML = 'x'` pero no para inyección rica).
  - **`__puriy_drain_dirty()`**: helper JS que devuelve las mutaciones acumuladas como string delim-codificado (U+001E entre campos, U+001F entre entries). Sin `JSON.stringify` desde el host para no sumar dependencias ni dolores de unicode escapes. Vacía el buffer atómico al drenar.
  - **`JsRuntime::drain_dom_mutations() -> Vec<DomMutation>`**: API del host. `DomMutation { id, kind, value }`. Llamado por el chrome después de cada `eval()` / `tick()` / `dispatch_event()`. Idempotente — dos drains seguidos sin operaciones intermedias devuelven `[]`.
  - **`BoxTree::set_element_text_content(id, new_text) -> bool`** en `puriy-engine`: implementa el caso simple de `textContent = X`. (1) si el nodo target tiene `.text` directo, lo reemplaza; (2) sino, reemplaza el PRIMER text leaf del subárbol en orden DFS — los hijos no-text quedan intactos. Devuelve `true` si aplicó. Spec real es "reemplazar TODO el subárbol con un text node único", lo que requeriría sintetizar un BoxNode con estilo del padre — esa parte queda para cuando aparezca el caso real.
  - **Cableado en el chrome (`puriy-llimphi`)**: nueva helper `apply_dom_mutations(t)` que drena y aplica al `t.box_tree`. Se llama después de `run_scripts_on_tab` (mutaciones de scripts iniciales), `dispatch_js_event` (mutaciones de handlers), y `tick_js_runtimes` (mutaciones de setTimeout/setInterval). El árbol mutado dispara re-render automático en el siguiente frame del bucle Llimphi.
  - **`set_elements` resetea `__puriy_dirty`**: una recarga de página construye un snapshot nuevo; el buffer de mutaciones de la sesión anterior NO debe filtrarse al box_tree nuevo.
  - **Limitaciones explícitas**: (1) `textContent` reemplaza el PRIMER text leaf, no todo el subárbol — diverge de spec pero matchea el 90% de los casos reales. (2) `innerHTML` no parsea HTML — el chrome trata el string como texto plano. (3) Sin `appendChild`/`removeChild`/`createElement` — la estructura del árbol está fija al load; sólo cambia el texto. (4) Sin observación inversa: si el HTML del documento cambiara, el `_textContent` del JS NO se actualiza (es snapshot por id, no proxy).
  - Tests nuevos: 7 en `puriy-js` + 3 en `puriy-engine` + 3 en `puriy-llimphi`. 307/307 verde (15 core + 212 engine + 54 js + 26 llimphi).
  - **Pendiente Fase 7.6+**: eventos no-click (keydown/input/change/focus); `event.preventDefault()` y `event.stopPropagation()`; `event` object como argumento; bubbling/capturing; querySelector con selectores compound; mutaciones de estructura (`createElement`/`appendChild`); estilo dinámico (`el.style.color = ...`); cohabitación link+handler para `<a>` con id.
- **Fase 7.5b (2026-05-29):** **Event handlers — `onclick`, `addEventListener`, `removeEventListener` sobre elementos con `id=`.** Segundo sub-paso de Fase 7.5: scripts ahora reaccionan a clicks del usuario, no sólo a `setTimeout`. Cubre `<button id=> onclick=`, `<div id=> onclick=`, `getElementById('x').addEventListener('click', fn)` — el patrón estándar de UI interactiva.
  - **Bootstrap `EVENTS_BOOTSTRAP` en `puriy-js`**: `globalThis.__puriy_elements = {}` + `__puriy_dispatch(id, type) -> count`. Despacha primero la propiedad `on<type>` del elemento (si es función), luego cada listener en `_listeners[type]` (registrados por `addEventListener`). Excepciones DENTRO de un handler van a stderr sin abortar el dispatch. `count` cuenta handlers DISPARADOS (haya o no haya tirado) — útil para que el chrome decida fallback.
  - **`JsRuntime::set_elements(&[ElementSnapshot])`**: nuevo método que reemplaza `globalThis.__puriy_elements`. Cada snapshot lleva `id`/`tag_name`/`text_content` (concatenación de las hojas de texto del subárbol). El elemento JS expuesto define `addEventListener`/`removeEventListener` y permite asignar `onclick`/`onkeydown`/etc. como propiedades directas. Idempotente — un load nuevo lo resetea.
  - **`JsRuntime::dispatch_event(element_id, event_type) -> u32`**: API del host. Devuelve cuántos handlers corrieron. Elemento inexistente o sin handlers → 0 (sin error).
  - **`set_document` actualizado**: `getElementById(id)` y `document.querySelector('#x')` ahora consultan `__puriy_elements` en lugar de devolver `null`. Selectores no-id siguen devolviendo `null` por ahora (cualquier `.class`/`tagname` requiere un índice secundario que se construirá cuando aparezca el caso real).
  - **Chrome (`puriy-llimphi`)**: `Msg::JsDispatchEvent { element_id, event_type }` se arma en `render_box` cuando un nodo tiene `element_id` no-vacío Y NO tiene link. Click dispara JS dispatch; pestañas sin runtime se saltean silenciosamente. `collect_element_snapshots(box_tree)` walka el árbol y arma el snapshot post-load. `node_text_content(node)` concatena las hojas del subárbol con separadores espacio.
  - **Limitaciones explícitas**: (1) `<a id=> href=>` con onclick — el link gana, el handler JS no se dispara. preventDefault y la cohabitación link+handler quedan para una fase posterior. (2) sólo evento `click` por ahora — `keydown`/`input`/`change`/`focus` enchufarán en sub-fases siguientes cuando lleguen los casos. (3) bubbling no implementado — el evento se dispara SÓLO sobre el elemento clickeado, no sobre sus ancestros. (4) `event.preventDefault()` / `event.stopPropagation()` no existen — los handlers reciben `undefined` como argumento, no un `Event` object.
  - Tests nuevos: 11 en `puriy-js` (`get_element_by_id_devuelve_el_indexado`, `query_selector_id_consulta_indice`, `add_event_listener_se_registra_y_dispara`, `onclick_property_se_dispara_igual_que_listener`, `onclick_y_listeners_disparan_ambos`, `dispatch_sobre_id_inexistente_devuelve_cero`, `remove_event_listener_cancela`, `error_en_handler_no_aborta_los_siguientes`, `set_elements_reset_borra_los_anteriores`, `handler_puede_registrar_timer_que_se_dispara_despues`) + 4 en `puriy-llimphi` (`collect_element_snapshots_indexa_solo_los_con_id`, `collect_element_snapshots_text_content_concatena_subarbol`, `dispatch_js_event_corre_handler_y_acumula_logs`, `dispatch_sobre_id_sin_handler_no_panic`). 294/294 verde (15 core + 209 engine + 47 js + 23 llimphi).
  - **Pendiente Fase 7.5c**: DOM mutation reactiva — `el.textContent = '...'` debe re-pintar el chrome (hoy queda en el objeto JS pero el view tree no lo ve). Eventos no-click. preventDefault. querySelector con selectores compound.
- **Fase 7.5a (2026-05-29):** **`setTimeout` / `setInterval` / `clearTimeout` con reactor poll.** Primer sub-paso de Fase 7.5 (timers + event handlers + DOM reactivo). Scripts ahora pueden esperar y reaccionar después del load — abre la puerta a polling clocks, banners rotativos, lazy-init y todo lo que asume "JS sigue corriendo después del paint".
  - **Harness JS-puro en `puriy-js::TIMERS_BOOTSTRAP`**: `globalThis.setTimeout(cb, ms)`, `setInterval(cb, ms)`, `clearTimeout(id)`, `clearInterval = clearTimeout`. Storage en `globalThis.__puriy_timers.queue` indexado por id auto-incremental. Cada entry guarda `{fire_at, callback, interval_ms|null}`. Callbacks aceptan `typeof === 'function'` (lo común) y `typeof === 'string'` (legacy — `(1, eval)(s)` lo fuerza al scope global). Mismo molde JS-puro que `console` de Fase 7.2 — sin function pointers Rust al guest, que el `.wasm` fijo no acepta.
  - **`__puriy_tick(now)`**: itera el queue ordenado por `fire_at` y dispara cada vencido **a lo más una vez por tick** (no catch-up — matchea browsers reales y previene loops infinitos con `setInterval(_, 0)`). Excepciones dentro de un callback van a `__puriy_stderr` sin abortar el tick. Intervals reprograman `fire_at = now + interval_ms`; timeouts se borran.
  - **`JsRuntime::tick(now_ms) -> TickResult { fired, remaining }`**: API del host. Bumpea `globalThis.__puriy_now_ms`, llama `__puriy_tick(now)`, lee el count de timers vivos. `pending_timers()` permite chequear el state sin tickear. `set_now_ms(now_ms)` se llama también desde el chrome ANTES de cada `eval()` de un script de carga, para que un `setTimeout(fn, 100)` registrado durante el load se ancle al reloj real (sin esto, fire_at sería 100ms desde t=0 y dispararía al instante).
  - **Chrome (`puriy-llimphi`)**: `Model` ahora lleva `start: Instant`. `init` arranca un `Handle::spawn_periodic(33ms, || Msg::JsTick)` — un solo thread global que sobrevive a la sesión. `Msg::JsTick` → `tick_js_runtimes(m, m.start.elapsed_ms())` walka cada `TabState.js.as_mut()`, saltea pestañas sin runtime (cost ~ns) y sin timers vivos (cost ~µs), y avanza los demás. Logs y errores del tick siguen acumulándose en `t.js_summary` (mismo conteo de líneas nuevas que `run_scripts_on_tab`).
  - **`JS_POLL_PERIOD_MS = 33`**: ~30 fps. Browsers reales clampan `setTimeout(_, 0)` a ~16ms; nosotros a 33 para no inundar el UI thread con ticks vacíos. La mayoría de los timers usan delays >> 33ms y ni notan la diferencia.
  - **Limitaciones explícitas**: (1) sin catch-up — un poll atrasado dispara cada timer a lo más una vez (intencional). (2) sin `requestAnimationFrame`/`requestIdleCallback` — vendrán cuando haya casos reales. (3) timers comparten el `JsRuntime` con scripts de carga: si un eval atrapa fuel, los timers comparten el mismo cap. (4) callbacks se ejecutan en el UI thread del chrome (mismo runtime) — un callback pesado bloquea pintado; quiénes hagan SPAs van a sentir esto y migraremos a un worker async cuando duela.
  - Tests nuevos: 10 en `puriy-js` (`set_timeout_dispara_al_tick_correcto`, `set_interval_se_reprograma_y_dispara_repetido`, `clear_timeout_cancela_antes_de_fire`, `clear_interval_para_el_loop`, `interval_no_hace_catch_up_por_tick`, `callback_string_se_evalua_en_scope_global`, `error_en_callback_no_crashea_el_tick`, `pending_timers_reporta_count_correcto`, `set_timeout_zero_dispara_al_proximo_tick`, `timers_respetan_now_ms_del_host_no_clock_real`) + 4 en `puriy-llimphi` (`tick_dispara_settimeout_pendiente`, `tick_no_panic_en_pestana_sin_js`, `tick_acumula_errores_en_summary`, `tick_continua_disparando_interval`). 280/280 verde (15 core + 209 engine + 37 js + 19 llimphi).
  - **Pendiente Fase 7.5b**: event handlers (`onclick`, `addEventListener`) — primero stubs que no se disparan, después dispatch real desde el chrome cuando el usuario clickea/teclea sobre un nodo con handler. Después de eso, DOM bindings reactivos (queries contra el árbol vivo, no snapshot).
- **Fase 7.4 (2026-05-29):** **Scripts externos (`<script src="...">`) descargados en paralelo dentro del worker.** Hasta Fase 7.3 inclusive, scripts con `src=` se salteaban — sólo se ejecutaba inline. Ahora el engine baja los externos junto con el HTML, los inyecta como inline y el chrome los corre sin distinguirlos.
  - **Crate `puriy-engine::scripts`**: nuevo módulo con `fetch_externals(scripts, base)`. Resuelve cada `src` contra la URL base del documento, filtra a `http`/`https` (descarta `data:`/`file:`/`about:`/`javascript:` — entrarán por puentes específicos cuando haga falta), saltea `is_module=true` y los que ya traen `inline` poblado.
  - **Paralelo con pool fijo**: `SCRIPT_FETCH_WORKERS = 6` (mismo patrón que `prefetch_image_urls`). Si la página tiene 1 script, va un thread; si tiene 20, van 6 con chunks de ~3-4. Sin `rayon`, sólo `std::thread::spawn`. Cada worker llama `fetch::fetch_bytes` que ya pasa por la cache LRU/TTL — recargas y back/forward son instantáneas.
  - **Mismo flujo de ejecución**: el body UTF-8 fetcheado se inyecta en `ScriptInfo.inline` (el `src` queda intacto por si el chrome quiere mostrarlo en logs). Cuando `Msg::Loaded` llega al chrome, `run_scripts_on_tab` no distingue inline original vs externo descargado — ejecuta los dos por igual. Network failures dejan `inline = None` y los saltea silenciosamente (no son errores JS, son fallas de red).
  - **Cableado en `Engine::load_with_referer` + `load_post_with_referer`**: tras `load_html` que construye el `Document`, llamamos `scripts::fetch_externals(&mut doc.scripts, &doc.url)`. `load_html` síncrono (test path con HTML en memoria) NO fetchea — eso permite seguir testeando el pipeline sin red.
  - **Orden DOM preservado**: el caller los recorre en el mismo orden que vinieron de `collect_scripts`, y como los fetch resultan dentro de `fetch_externals` indexados por su `i`, la inyección preserva la posición original.
  - Tests nuevos: 7 en `puriy-engine::scripts` (`collect_resuelve_relativos_contra_base`, `collect_filtra_no_http`, `collect_saltea_modules`, `collect_saltea_sin_src`, `collect_saltea_si_ya_hay_inline`, `fetch_externals_base_invalida_no_panic`, `fetch_externals_sin_candidatos_no_lanza_threads`). 266/266 verde (15 core + 209 engine + 27 js + 15 llimphi).
  - **Pendiente Fase 7.5**: event handlers (`onclick`, `addEventListener`), `setTimeout`/`setInterval` con un reactor que el chrome poll-ea, y eventualmente DOM bindings real-time con queries que reflejan el árbol vivo (no snapshot). El attribute `defer`/`async` lo capturamos en `ScriptInfo` pero el ordering todavía es "todos en orden DOM antes del paint" — el split defer-vs-async correcto requerirá meter el async fetch ANTES de que el chrome muestre el `Loaded`.
- **Fase 7.3 (2026-05-29):** **Chrome ejecuta scripts inline post-load.** El cableado final que cierra el flujo Fase 7: el chrome (puriy-llimphi) consume `Document.scripts` que Fase 7.0 expuso y los ejecuta en el `JsRuntime` que Fase 7.2 dejó listo.
  - **`TabState.js: Option<Box<JsRuntime>>`**: runtime por pestaña, lazy (sólo se instancia si la página tiene scripts ejecutables — ~200ms de cost). `Box` para no inflar el `Vec<TabState>` con ~1MB cuando la mayoría de pestañas no usan JS. `JsRuntime` no es `Send` — el `Model` vive en el UI thread y nunca cruza hilos.
  - **`TabState.js_summary: JsSummary { logs, errors }`**: contador agregado del último batch — se muestra en la status bar como `· JS: N logs` o `· JS: N log/M err` cuando hay errores. Se resetea en cada `Msg::Loaded`.
  - **`Msg::Loaded` ahora lleva `scripts: Vec<ScriptInfo>`**: los dos sites que lo dispatch (load síncrono + POST) pasan `doc.scripts` (que Fase 7.0 ya recolectaba).
  - **`run_scripts_on_tab(t, scripts)`**: helper que (1) saltea si no hay scripts ejecutables (todos los inline son `is_module=true`, o todos tienen `src=`); (2) instancia un `JsRuntime` fresh, destruyendo cualquier previo (var de la página anterior NO fuga); (3) llama `set_document(title, url, body_text)` donde `body_text` viene de `extract_body_text(box_tree)` — concatenación de las hojas de texto con separadores espacio; (4) eval cada script inline en orden, contando líneas nuevas en `stdout`/`stderr` (cada `console.log` agrega exactamente un `\n`); (5) salta scripts con `type=` no-JS (`application/json`, `text/template`, etc.) pero acepta `text/javascript`/`text/ecmascript`/`text/js`.
  - **Filtro por tipo**: scripts `is_module=true` (ES modules) se saltean — el runtime de Fase 7.x es clásico, no module loader. Scripts con `src=` también se saltean — Fase 7.4+ tendrá async fetch + queue de ejecución con priorities (defer/async/sync).
  - **Snapshot read-only del DOM**: `document.title`, `document.URL`, `document.body.textContent` reflejan el estado al momento del load. Si la página re-renderiza después, el JS NO ve la actualización — Fase 7.5+ enchufará un DOM real-time con queries contra el árbol vivo.
  - Tests nuevos: 6 en puriy-llimphi (`extract_body_text_concatena_hojas`, `run_scripts_actualiza_summary_logs`, `run_scripts_captura_error_thrown`, `run_scripts_saltea_modules_y_src_externo`, `run_scripts_documento_inyecta_title_y_url`, `run_scripts_skip_application_json_pero_no_text_javascript`). 259/259 verde (15 core + 202 engine + 27 js + 15 llimphi).
  - **Pendiente Fase 7.4**: scripts externos con async fetch (asignar al worker thread del load + ordering con defer/async/sync), event handlers (`onclick`, `addEventListener`), `setTimeout`/`setInterval` con un reactor que el chrome poll-ea, y eventualmente DOM bindings real-time con queries que reflejan el árbol vivo (no snapshot).
- **Fase 7.2 (2026-05-29):** **`console.log` + DOM bindings read-only via JS-puro.** Cableado de hostcalls sin function pointers Rust al guest (que el `.wasm` fijo no acepta). El truco: indirección por buffers JS globales que el host drena.
  - **Bootstrap `console`**: al final de `JsRuntime::new`, se eval-ea un script que define `console = { log, info, debug, error, warn }`. `log/info/debug` appendean a `globalThis.__puriy_stdout`; `error/warn` a `globalThis.__puriy_stderr`. Soporte para múltiples argumentos (join con espacio), `null`/`undefined` literales, y objetos via `JSON.stringify` (try/catch a String si falla — refs circulares).
  - **`eval()` ahora drena IO**: tras ejecutar el código del usuario (con éxito o fallo), llama internamente a `DRAIN_IO` — un script que lee los buffers, los vacía, y devuelve `stdout + NUL + stderr` concatenados. El host splittea por NUL y appendea a sus buffers `Arc<Mutex<String>>`. Si el eval del usuario lanzó, el log que escribió antes igual se conserva.
  - **`eval_raw()`**: nueva variante interna que NO drena IO. Se usa para el bootstrap (antes de que `__puriy_stdout` exista) y para el drain mismo (evitar recursión).
  - **`set_document(title, url, body_text)`**: snapshot read-only del DOM. Inyecta `globalThis.document = { title, URL, readyState: 'complete', body: { textContent, innerHTML }, getElementById, querySelector, querySelectorAll }` + `globalThis.window = globalThis` + `globalThis.location = { href }`. `getElementById`/`querySelector` son stubs que devuelven `null` por ahora — Fase 7.3 los enchufa a queries reales contra el `BoxTree`. Idempotente: llamar varias veces resetea los valores.
  - **`js_string_literal(s)`**: helper público — convierte un `&str` arbitrario a un literal string JS válido (escape de `"`, `\`, `\n`, `\r`, `\t`, `\b`, `\f`, control chars `< 0x20`, U+2028, U+2029). Pegable directo en scripts generados desde el host sin riesgo de injection. Usado por `set_document` para inyectar strings del DOM seguros.
  - **`JsRuntime` sigue sin ser `Send/Sync`**: el `wasmi::Store` no lo es. Para cablear con el chrome (Fase 7.3) habrá que mantener el runtime en el `Model` del App (que vive en el UI thread) o serializar el access desde un worker.
  - Tests nuevos: 14 (`console_log_captura_a_stdout`, `console_log_multiples_args`, `console_error_captura_a_stderr`, `console_log_acumula_entre_evals`, `clear_io_vacia_buffers`, `console_log_objeto_es_json_stringify`, `console_log_capturado_incluso_si_eval_falla_despues`, `set_document_define_title_y_url`, `set_document_define_body_textcontent`, `set_document_getElementById_devuelve_null`, `set_document_window_es_globalthis`, `set_document_escapa_strings_seguro`, `js_string_literal_escapa_chars_basicos`, `js_string_literal_escapa_unicode_separators`). 253/253 verde (15 core + 202 engine + 27 js + 9 llimphi).
  - **Pendiente Fase 7.3**: cablear con el chrome (puriy-llimphi) — crear un `JsRuntime` por pestaña, llamar `set_document` post-load, ejecutar scripts inline de `Document.scripts`, mostrar logs en algún lugar visible (status bar/panel).
- **Fase 7.1 (2026-05-29):** **QuickJS-NG corriendo sobre wasmi — `JsRuntime::eval()` ejecuta JS real.** El stub de Fase 7.0 ahora ejecuta JavaScript real. Sin cambios al API público (mismo shape `eval(&str) -> Result<JsValue, JsError>`).
  - **Binario embebido**: `qjs-wasi-reactor.wasm` (1.52 MB) de [`quickjs-ng/quickjs` v0.15.0](https://github.com/quickjs-ng/quickjs/releases/tag/v0.15.0) — el reactor expone toda la API C de QuickJS (`JS_Eval`, `JS_NewRuntime`, `JS_NewContext`, `JS_ToCStringLen2`, `JS_ToFloat64`, `JS_FreeValue`, etc.) como exports WASM. `include_bytes!("runtime/qjs-wasi-reactor.wasm")` versiona el .wasm con el crate — diff binaria visible en review cuando upstream actualiza.
  - **wasmi 1.0 setup**: `Config::default().consume_fuel(true)`, `Engine::new`, `Module::new`, `Linker` + 21 WASI stubs, `instantiate_and_start`, `_initialize()`. Default fuel = 50M (corta loops infinitos en ~1s wall-clock).
  - **Atajo `qjs_init`**: NO usamos el helper `qjs_init` del wrapper de quickjs-ng — devuelve NULL porque intenta setear los modules `std`/`os` que requieren WASI features pesadas (filesystem, environ_get reales). En su lugar llamamos `JS_NewRuntime` + `JS_NewContext` directos. Browser embebido NO necesita modules de host I/O.
  - **WASI stubs (21 funciones)**: `fd_write(1, ...)` captura stdout, `fd_write(2, ...)` stderr, `clock_time_get` real desde `std::time`, `environ_*` 0 vars, `proc_exit` trap. El resto (`path_*`, `fd_prestat_*`, `fd_seek`, `fd_readdir`, `fd_close`, `fd_fdstat_*`, `poll_oneoff`, `fd_read`) devuelven errno apropiado (BADF/EOF/NOSYS) — un browser no toca disco.
  - **Decodificación de `JSValue`**: la NaN-boxing de QuickJS-NG aplica un addend al alto 32 que no es trivialmente decodificable desde el host. En lugar de reimplementarla, discriminamos por tag los tipos conocidos (`UNDEFINED=3`, `NULL=2`, `BOOL=1`, `INT=0`, `STRING=-7`, `OBJECT=-1`, `EXCEPTION=6`) y para cualquier otro tag llamamos `JS_ToFloat64(ctx, &out, val)` — extrae el double via FFI sin pelearse con el bit-packing. Coste: una llamada wasmi extra por float, despreciable en eval típicos.
  - **`JsValue::Object` todavía no existe**: objetos/arrays/funciones se devuelven como `JsValue::String(toString_result)` por ahora. Fase 7.2+ expondrá handles tipados con DOM bindings.
  - **Persistencia entre evals**: variables globales sobreviven (`eval("var x = 10")` seguido de `eval("x * 2")` devuelve 20). El `JSContext` se mantiene vivo en el `JsRuntime`.
  - Tests nuevos: 11 (eval aritmética, string literal, undefined/null, booleanos, floats, persistencia, syntax error, throw explícito, reference error, objeto-coerce-string, runtime arranca sin eval). 239/239 verde (15 core + 202 engine + 13 js + 9 llimphi).
  - **Pendiente Fase 7.2**: enchufar `console.log` real (los modules `std`/`os` de QuickJS lo proveen pero los salteamos; tendremos que cablear nuestra propia función JS `console = { log: ... }` vía `JS_NewCFunction2`), DOM bindings read-only (`document.querySelector`, `el.textContent`), y consumir `Document.scripts` desde el chrome.
- **Fase 7.0 (2026-05-29):** **JS embebido — scaffold del crate `puriy-js` + detección de `<script>` en el engine.** Apertura de Fase 7 (la grande: motor JavaScript embebido). Esta fase NO ejecuta JS todavía; sólo deja el cableado listo para que Fase 7.1 enchufe el runtime real.
  - **Crate nuevo `puriy-js`** (00_unanchay/puriy/puriy-js): API estable `JsRuntime`/`JsValue`/`JsError`. Por ahora un **stub**: `eval(_)` acepta el source y devuelve `JsValue::Undefined` sin ejecutarlo. `JsValue` cubre los escalares (Undefined/Null/Bool/Number/String) con `to_display_string()` (coerción al estilo `String(v)` de JS) y `to_bool()` (truthy/falsy spec). `JsError` modela las cuatro familias que llegarán: `Runtime` (excepciones), `Syntax` (SyntaxError de parser), `OutOfFuel` (cap wasmi), `NotSupported` (features fuera del runtime actual). `DEFAULT_FUEL = 5_000_000`. El shape del API NO cambia entre fases — los callers escritos contra el stub seguirán funcionando.
  - **`DomTree::collect_scripts() -> Vec<ScriptInfo>`** en `puriy-engine`. Walka el DOM in-order, extrae cada `<script>` con `src: Option<String>`, `inline: Option<String>` (mutuamente exclusivos — HTML spec ignora el body si hay src), `type_attr: Option<String>`, `is_module: bool` (true si `type="module"`), `defer: bool`, `async_: bool`. Scripts vacíos (sin src ni body) se dropean al recolectar para que el caller no itere sobre nada. `Document` ahora lleva `pub scripts: Vec<ScriptInfo>` poblado en `load_html`.
  - **Decisión arquitectónica reafirmada** (alineada con `[[project-notebook-runtimes-wasm]]` y `[[project-wawa-gaming]]`): el runtime JS será **QuickJS compilado a WASM**, ejecutado vía `wasmi` (interprete) hoy y migrable a `wasmtime + cranelift JIT` sin tocar el `.wasm` guest. Razones: (1) aísla el motor en sandbox WASM — la pila Rust del host queda intacta; (2) porta a wawa idéntico — el SO bare-metal ya corre `.wasm` con wasmi para todas sus apps; (3) evita FFI a C++ — `rusty_v8` ata a una versión de V8 y un blob inmutable no fuerza recompiles del host.
  - **Render no cambia**: `<script>` mantiene `Display::None` en la UA stylesheet — el contenido inline no se renderiza visualmente. La pipeline visual de Fase 6.6 sigue intacta.
  - Tests nuevos: 6 en `puriy-js` (defaults del runtime, fuel custom, stub devuelve Undefined, `to_display_string` para todos los escalares + NaN + Infinity, `to_bool` truthy/falsy, `clear_io` no crashea) + 6 en `puriy-engine::dom` (script inline detectado, script src+attrs, body ignorado cuando hay src, `type="module"` distinguido, scripts vacíos dropeados, orden DOM preservado). 232/232 verde (15 core + 202 engine + 6 js + 9 llimphi).
- **Fase 6.6 (2026-05-28):** **Sweep "todos los pendientes"** — 5 features encadenados que cierran la línea de generated content / pipeline de carga / rendering defaults.
  - **Default line-height 1.2** (era 1.4): CSS spec `normal` es ~1.2 en browsers reales. Reducción global del espacio vertical entre líneas, render más compacto. Cambia los 5 sitios con `unwrap_or(1.4)` (engine boxes + chrome layout + chrome decorations + chrome details flecha).
  - **Font-family default `serif`**: UA stylesheet ahora declara `body { font-family: serif }`. Parley resuelve la family genérica `serif` contra la system font config (Times New Roman / DejaVu Serif / etc. según OS). Match con browser defaults — antes Llimphi caía a sans-serif system default, que da look "moderno" pero rompe la expectativa de raw HTML.
  - **Prefetch de `background-image` post-style**: segundo pass paralelo después del primer (`<img>`/`<picture>`). Walk del DOM computando style por nodo (sin parent — background-image no se hereda, así que el value es independiente), recolecta URLs, dedupe, filtra schemes no-HTTP + ya cacheadas, descarga en chunks por workers. Mismo pool `PREFETCH_WORKERS=6` que el primer pass.
  - **`url()` en `content:`**: `ContentItem::Url(String)` + parser que acepta `url("...")`, `url('...')`, `url(...)`. Strippea comillas y trimea. `resolve_content_items` ignora urls (sólo concatena strings); el dispatch real está en `emit_content_items` del builder de boxes, que separa items en runs: text/counter/attr se concatenan a un solo `inline_text_with_style`; cada url genera un `synthetic_image_box` (inline-block con `b.image = Some(decoded)`). Orden DOM preservado. Url que falla al decode se omite (matchea CSS spec: pseudo invalido suprime su materialización).
  - **`<iframe>` placeholder**: antes `Display::None` ocultaba el elemento entero. Ahora `Display::Block` con UA stylesheet (border gris #b4b4b4 + bg #f8f8f8 + padding 8px + color #646464 + margin vertical 8px) que da el look "embed placeholder", más text leaf con `[iframe: <src>]` (o `[iframe sin src]` si no tiene). El lector ve QUE hay contenido embebido y a dónde apunta. canvas/math/video/audio/object/embed siguen `Display::None` por ahora.
  - Tests nuevos: 3 (`iframe_se_renderea_como_placeholder_con_url`, `iframe_sin_src_muestra_label_generico`, `content_url_parser_acepta_quoted_y_unquoted`). 220/220 verde (196 engine + 15 core + 9 llimphi).
- **Fase 6.5 (2026-05-28):** **Margin collapsing contra el padre** — completa el modelo iniciado en 6.4 con el caso CSS spec restante. Antes: `<body style="margin: 8px">` con primer hijo `<h1 style="margin: 21px 0">` mostraba 8 + 21 = 29px arriba; ahora colapsa a `max(8, 21) = 21px`, que es lo que pintan los browsers.
  - **`collapse_margins_against_parent`**: si el padre no tiene `border-top` ni `padding-top` (no hay "barrera" entre él y el primer hijo), promueve `child.margin_top` al padre (`parent.margin_top = max(parent.margin_top, child.margin_top)`) y setea el del hijo a `0`. Idem para `last child` y bottom. Solo aplica si el padre es `Display::Block` — Flex/Grid/Inline establecen contexto distinto que CSS spec dice que NO colapsa.
  - **Aplicado solo en el branch Element**: el wrapper Document (fallback con margin 0) NO lo aplica — su margin no se ve en la salida, y promover el margin del primer hijo lo perdería en cascada.
  - **Tests**: `margin_collapse_padre_promueve_margin_del_primer_hijo`, `margin_collapse_padre_bloqueado_por_padding`. 217/217 verde (193 engine + 15 core + 9 llimphi).
  - **Bonus**: fix de workspace roto — un commit anterior (`c713a9a feat(agora): agora-app pasa de CLI demo a UI Llimphi`) eliminó la carpeta `agora-cli` sin actualizar `Cargo.toml`. Quitada la entrada huérfana del workspace para destrabar `cargo check --workspace`.
- **Fase 6.4 (2026-05-28):** **Fixes de raw HTML rendering** — primer pass contra páginas sin CSS de autor (motherfuckingwebsite.com como referencia ácida) destapó 3 issues que el modelo CSS naïve dejaba pasar.
  - **Margin collapsing entre block siblings**: el bug mayor — sin colapsar, dos `<p>` consecutivos con `margin: 1em 0` mostraban gap del doble. CSS spec dice que entre hermanos block adyacentes el gap vertical es `max(prev.margin_bottom, next.margin_top)`, no la suma. Nuevo `collapse_vertical_margins(children)` que, post `strip_block_adjacent_whitespace`, reduce `next.margin_top` por `min(prev.margin_bottom, next.margin_top)`. Total final: `prev.margin_bottom + (next.margin_top - reduction) = max(prev.margin_bottom, next.margin_top)`. Casos NO cubiertos: collapse con el padre (cuando primer/último hijo no tiene padding/border), negative margins, through-block collapsing en blocks vacíos. Cubre el 90% visual.
  - **Trailing whitespace runs**: `strip_block_adjacent_whitespace` antes miraba sólo el vecino inmediato; runs consecutivas de whitespace inlines al final del body sobrevivían (post `</blockquote>` el HTML deja `\n  \n` → 2-3 text nodes). Ahora cada whitespace busca el primer vecino *no-whitespace* (skip runs). motherfucking baja de 66 a 63 boxes.
  - **Bullets con espacio único**: `li_marker` para Disc/Circle/Square emitía `"•  "` con dos espacios — el segundo era redundante porque el text leaf del `<li>` ya tiene su propio leading whitespace post-collapse. Reducido a `"• "`.
  - Tests nuevos: 2 (`margin_collapsing_max_entre_block_siblings`, `margin_collapsing_no_aplica_a_inline`). 215/215 verde (191 engine + 15 core + 9 llimphi).
- **Fase 6.3 (2026-05-28):** **Prefetch paralelo de imágenes** — corrección estructural de la pipeline de carga. Antes `build_node` síncrono descargaba cada `<img>`/`<picture>` en serie dentro del DFS, así que una página con 20 imágenes hacía 20 round-trips serializados — segundos de latencia antes de pintar nada. Ahora un pre-walk del DOM recolecta todas las URLs absolutas y dispara descargas en paralelo con un pool de 6 workers (mismo bound que browsers reales por host). El `build_node` síncrono después hace cache hit y termina inmediato.
  - **`prefetch_image_urls(root, base)`**: walka el DOM una vez, recolecta URLs de `<img src>`/`<img srcset>`/`<picture><source srcset>` (resueltas contra base, deduplicadas via `HashSet`), filtra schemes no-HTTP (`about:`/`file:`/`data:` no deben sangrar al pool — sería round-trip al timeout) y URLs ya cacheadas. Las restantes se dividen en chunks por número de workers; un `std::thread::spawn` por chunk. Sin deps de `rayon` ni de `tokio` — la división estática alcanza para 6-30 URLs típicas.
  - **`PREFETCH_WORKERS = 6`**: alto enough para esconder latencia de TCP/TLS (handshake ~50-200ms cada uno), bajo enough para no saturar servidores ni el ulimit de sockets del proceso. Browsers reales usan 6-8 por host — alineado.
  - **`background-image: url(...)` queda fuera** del pre-walk por ahora — vive en CSS y requiere computar styles primero. Sigue descargando síncrono dentro de `build_node`. Cuando aparezca un caso real con muchos, lo movemos a un segundo prefetch pass post-style.
  - **Best-effort**: errores de fetch se ignoran silenciosamente; `build_node` posterior reintentará serializado (cache miss → fetch) y mostrará el `alt` del `<img>` si igual falla. No cambia el contrato del fetch sync.
  - Tests nuevos: 2 (`prefetch_no_crashea_sin_imagenes`, `prefetch_skip_de_urls_no_http` con bound de 500ms que confirma que el filtro evita round-trips innecesarios). 213/213 verde (189 engine + 15 core + 9 llimphi).
- **Fase 6.2 (2026-05-28):** **CSS counters + `attr()` en content** — extiende los pseudo-elements de 6.1 hacia generated content estructural: numbered headings/sections sin JS, badges con atributos del DOM, breadcrumbs.
  - **`ContentItem` enum** — `ComputedStyle.content` pasó de `Option<String>` a `Option<Vec<ContentItem>>`. Items: `Text(String)`, `Counter(String)`, `Attr(String)`. Permite concatenación tipo `content: "Sección " counter(sec) ": " attr(data-title)`.
  - **Parser de content**: tokenizer de items reconoce string quoted (single/double, escape `\\`), `counter(name)` y `attr(name)`. `counter(name, list-style)` toma sólo el name (el list-style queda para más adelante). `url(...)`, `counters(...)` aún no soportados — el value entero cae a None si los menciona. Helpers nuevos: `parse_string_literal`, `read_ident`, `read_until`.
  - **`counter-reset` y `counter-increment`**: parser `parse_counter_list` acepta pares `name [val] name2 [val2]...` con defaults (reset=0, increment=1) cuando el value está implícito. `none` resuelve a vec vacío. `ComputedStyle` lleva ambos como `Vec<(String, i32)>`.
  - **`build_node` aplica counters**: lleva un `&mut HashMap<String, i32>` por todo el DFS. Al entrar a un nodo: aplica `style.counter_reset` (insert/overwrite) y luego `style.counter_increment` (entry().or_insert(0) + delta). Cuando computa el pseudo `::before` o `::after`, también aplica `ps.counter_reset`/`ps.counter_increment` ANTES de resolver content — esto destraba el patrón clásico `h2::before { counter-increment: sec; content: counter(sec) }` donde el increment vive en la regla del pseudo, no en la del `<h2>`. Implementación pragmática: counter scope global (no por subárbol). CSS spec dice que reset crea scope nuevo por subárbol; eso requiere un stack y rara vez importa para los usos comunes — cuando importe se mete.
  - **`resolve_content_items`**: helper público que toma `&[ContentItem]`, el `&Handle` del nodo padre del pseudo y el `&HashMap` de counters; devuelve la string concatenada. Counters ausentes resuelven a `0` (matchea CSS spec — el implícito vale 0). Atts ausentes resuelven a `""`.
  - Tests nuevos: 4 (`parse_content_value_concat_counter_attr`, `parse_counter_list_acepta_pares_y_defaults`, `counter_numera_h2_sequencialmente`, `attr_en_content_lee_del_padre_del_pseudo`). Los 6 tests de Fase 6.1 actualizados a la nueva firma `Vec<ContentItem>`. 211/211 verde (187 engine + 15 core + 9 llimphi).
- **Fase 6.1 (2026-05-28):** **`::before` y `::after` pseudo-elements** — apertura de Fase 6. Esta fase deja de ser "polish CSS encima del MVP" (Fase 5) y empieza features estructurales que abren caminos a counters, generated content, iconos via content, badges y a futuras integraciones (JS engine en Fase 6.N+, async asset prefetch, etc.).
  - **Decisión sobre JS**: NO embebemos `script` de Servo. La línea acordada (alineada con [[project-notebook-runtimes-wasm]] y con el patrón de [[project-wawa-gaming]]) es **QuickJS compilado a WASM**. QuickJS (Bellard, C) es el motor JS pequeño más completo del ecosistema — ES2023, Symbol/Proxy/Reflect, async/await, BigInt, módulos reales; cosas que `boa` todavía no cubre. Vive aislado dentro de un `.wasm`, así que la pila Rust del repo se preserva en el runtime, no en el guest. Plan en dos pasos: (1) `wasmi` interpretado para arrancar, igual que `pluma-notebook-kernel-wasm`; (2) cuando importe throughput, migrar el runtime a **wasmtime + cranelift JIT** sin tocar el `.wasm` — mismo binario, 10-100× speedup. `rusty_v8` queda descartado (FFI a C++, ata a la versión de V8); `boa` queda como fallback si QuickJS-en-WASM resultara intolerablemente lento incluso con cranelift.
  - **`Selector.pseudo_element: Option<PseudoElement>`** — el parser strippea `::before`/`::after` (más el legacy `:before`/`:after`) del final del selector y los guarda separados. Reglas con pseudo_element sólo matchean cuando el caller pide ese pseudo; reglas sin pseudo matchean sólo el elemento real. Las reglas pseudo NO se filtran por sí solas al elemento (`p::before { color: red }` ya no afecta al `<p>` mismo — un bug del modelo previo que ahora queda cubierto).
  - **`compute_pseudo(node, pseudo, parent)`**: nueva variante de StyleEngine que computa el estilo del pseudo-element. Devuelve `None` cuando `content` no se setea — CSS spec dice que un pseudo sin content no se materializa.
  - **`content: "..."`**: nuevo `ComputedStyle.content: Option<String>` + `DeclKind::Content` + `parse_content_value` que acepta string quoted (single/double) con escape `\\`, más los keywords `none`/`normal` (que resuelven a `None`). `counter(...)`/`attr(...)`/`url(...)` quedan para Fase 6.2.
  - **boxes**: `build_node` después de computar el estilo del nodo, llama `styles.compute_pseudo(node, Before, Some(&style))`; si devuelve Some, inyecta `inline_text_with_style(content, &pseudo_style)` al inicio de `children` (antes del marker de `<li>` y de los hijos reales). Igual para `After`, appendeado al final. Reusamos el helper de hojas Text que ya respeta color/font/family/decoration heredados del pseudo style — no hace falta un constructor nuevo.
  - **Fix latente colateral**: `strip_comments` iteraba bytes y pusheaba `bytes[i] as char`, destruyendo runs UTF-8 multi-byte (un `▸` se convertía en 3 chars latin-1 basura). Ahora copia slices `&str` originales preservando UTF-8. Detectado por el test de `::before { content: "▸ " }` — la pipeline CSS estaba lookahead-safe pero output-broken para contenido no-ASCII.
  - Tests nuevos: 6 (engine: `parse_content_value_acepta_string_quoted`, `parse_content_value_respeta_escapes`, `pseudo_element_extrae_del_selector`, `pseudo_element_sin_content_no_se_materializa`, `reglas_pseudo_no_pegan_al_elemento_real`, `before_y_after_se_inyectan_como_children`). 207/207 verde (183 engine + 15 core + 9 llimphi).
- **Fase 5.1 (sgte+13):** **Sweep "moderno + download + middle-click + z-index"** — 4 features que quitan papelitos amarillos del backlog.
  - **CSS `calc()`**: parser `parse_calc_expr` reconoce `calc(<term> <+/-> <term>...)`. Resuelve en parse time: `calc(10px + 5px)` → `Px(15)`, `calc(80% - 10%)` → `Pct(70)`. Para mezclas `calc(100% - 20px)` conservamos el `Pct(100)` ignorando el offset px — taffy no soporta calc nativo y resolver con precisión requeriría el container width que no tenemos a parse-time. Tokenizer respeta el requisito de whitespace alrededor de `+`/`-` (CSS spec). `*`/`/` y `var()` adentro de calc no soportados aún. Acepta `calc(...)` en cualquier `LengthVal` (width/height/insets/margin/padding).
  - **`<a download>`**: `BoxNode.link_download: Option<String>` desde el attr. Chrome: `Msg::DownloadLink { url, filename_hint }` reemplaza al Navigate cuando el link tiene download. Spawn thread con `fetch_bytes` + `std::fs::write` a `$XDG_DOWNLOAD_DIR/puriy/<filename>` (fallback `$HOME/Downloads/puriy/`). `pick_download_filename` prefiere el hint si no contiene `/` ni `\` (anti-path-traversal); sino el último segmento del path de la URL; fallback `descarga`. Msg `DownloadDone` actualiza la status bar con `⬇ <path> · N bytes` o `⬇ fallo: <error>`. Gen counter invalida si la pestaña se cerró.
  - **Middle-click → new tab**: nuevo `llimphi_ui::View::on_middle_click(msg)` + `MountedNode.on_middle_click` + `hit_test_middle_click` + handler de `MouseInput { button: MouseButton::Middle }` en el runtime. Mismo modelo que right-click: la capa overlay tiene prioridad. Puriy: cualquier link (incluso los no-_blank) responde a middle-click con `NavigateNewTab` — convención estándar de browsers. Render_link_subtree también lo aplica para que children clickeables del link respondan igual.
  - **z-index real (positioned only)**: `ComputedStyle.z_index: i32` + `BoxNode.z_index` + parser de `z-index: N|auto`. Chrome `render_children_z_ordered` separa children en in-flow (orden DOM) y out-of-flow (position absolute/fixed); ordena los out-of-flow por z-index ascendente y los append al final del Vec — el mayor pinta encima. Reordenar out-of-flow es seguro porque su layout depende de insets, no del Vec order. Para `position: static`/`relative`/`sticky` el z-index se ignora (matchea CSS spec parcial).
  - Tests nuevos: 6 en engine (`parsea_calc_solo_px`, `parsea_calc_solo_pct`, `parsea_calc_mixto_pierde_offset_px`, `parsea_calc_invalido_devuelve_none`, `parsea_z_index`) + 1 en llimphi (`pick_download_filename_usa_hint_si_es_seguro`). 201/201 verde (177 engine + 15 core + 9 llimphi).
- **Fase 5.1 (sgte+12):** **Sweep "compat real + UX find/forms"** — 4 features que el browser real da por descontado y que sin ellas las páginas se ven raras o sienten tiesas.
  - **`<base href="...">` override**: `DomTree::base_href()` busca el primer `<base>` del head y devuelve su `href` (trimeado). `boxes::build` lo prefiere sobre la URL del documento al armar la `base: Option<Url>` que reciben `resolve_href` y compañía. Si el base es relativo, se resuelve contra la URL del doc antes de tomarlo. Sin él, fallback al comportamiento previo (Document.url como base). Wikipedia y muchas SPAs lo usan para anclar todos los relative links a un prefijo común.
  - **`autofocus` en input/textarea**: `BoxNode.input_autofocus: bool` poblado desde el attr. En `Msg::Loaded`, el chrome walkea el árbol y, al primer `input_kind.is_some() && input_autofocus`, setea `focused_input = Some(idx)`. Si hay varios autofocus el primero gana (matchea spec). El usuario empieza con el cursor en el campo correcto sin click.
  - **Find auto-scroll al match**: nuevo `BoxTree::find_y_of_match(query_lower, nth_1based)` que walkea pre-order contando text leaves cuyo lowercase contiene la query y devuelve la y aproximada del N-ésimo (mismo estimador que `find_element_y`: margin/padding de blocks + font_size×line_height por leaf). Chrome: helper `scroll_to_find_match` setea `scroll_y = y - 80px` (deja margen de contexto arriba) tras cada `FindNext`/`FindPrev`. Antes la barra contaba matches pero el usuario tenía que scrollear a mano para verlos.
  - **`Referer:` header**: nuevo `fetch::sanitize_referer` que acepta sólo `http`/`https` (no fugamos `about:`/`file:`/`data:`) y strippea el fragment (es estado del cliente). `fetch_with_referer`, `fetch_bytes_with_referer`, `post_form_with_referer` aceptan `Option<&str>`. `Engine::load_with_referer` y `load_post_with_referer` los exponen. Chrome: `start_load` y `start_load_post` capturan `t.url` antes de pisarlo y lo pasan como referer; `NavigateNewTab` también propaga el referer del tab fuente. Esto destraba sitios que validan Referer para anti-CSRF o que devuelven 403 cuando viene vacío.
  - Tests nuevos: 7 (engine: `base_href_extrae_del_head`, `base_href_ausente_o_vacio_devuelve_none`, `input_autofocus_se_marca_solo_para_inputs_con_attr`, `find_y_of_match_devuelve_y_creciente_por_match`, `sanitize_referer_acepta_http_y_https`, `sanitize_referer_strippea_fragment`, `sanitize_referer_rechaza_no_http`). 195/195 verde (172 engine + 15 core + 8 llimphi).
- **Fase 5.1 (sgte+11):** **Sweep "borders finos + meta refresh"** — borders per-side + per-corner + auto-refresh por `<meta>`. Hace que pasen el filtro las páginas con infoboxes Wikipedia-style, banners con esquinas asimétricas, y splash pages con redirect timer.
  - **Border per-side**: `ComputedStyle.border_widths: Sides<f32>` y `border_colors: Sides<Option<Color>>` (antes escalares). Nuevo `BorderEdge { Top, Right, Bottom, Left }` + `DeclKind::BorderSideWidth/Color/Style(edge, ...)`. Parser detecta `border-{top|right|bottom|left}` (shorthand del lado) y los longhand `border-{edge}-{width|color|style}`. El shorthand `border:` sigue seteando los 4 lados. Chrome: helper `apply_decorations` distingue (a) **uniforme** (los 4 lados con mismo width+color) → `RoundedRect::stroke` cómo antes para preservar suavidad de corners radius; (b) **per-side** → 4 `kurbo::Line::stroke` independientes, una por lado, cada una inseteada por w/2 dentro del rect. Las corners radius en el caso per-side quedan en chaflán cuadrado, que matchea el look estándar de browsers cuando los lados difieren.
  - **Border-radius per-corner**: `border_radii: Corners<f32>` con campos `top_left/top_right/bottom_right/bottom_left`. Nuevo enum `BorderCorner` + `DeclKind::BorderCornerRadius(corner, v)`. Parser detecta `border-{top|bottom}-{left|right}-radius`. El shorthand `border-radius:` sigue seteando las 4 esquinas. Chrome usa `radius_max = max(4 corners)` para `View::radius` (la API actual del llimphi-ui acepta sólo un escalar); cuando las 4 son iguales el resultado es exacto, cuando difieren el clip queda con la esquina más redondeada. Pintado per-corner real esperaría una API de `RoundedRectRadii` en llimphi-ui.
  - **`<meta http-equiv="refresh">`**: nuevo `DomTree::meta_refresh() -> Option<MetaRefresh { delay_secs: u32, url: Option<String> }>` que walkea metas y parsea el formato `content="N[;url=...]"` (case-insensitive, comillas opcionales, tolerante a `,` como separador). El parser ignora la parte fraccional del delay (`5.5` → `5`). `Document` expone `meta_refresh`. Chrome resuelve la URL contra `t.url` (base) y, en `Msg::Loaded`, programa un `std::thread::spawn(|| sleep + dispatch)` con `Msg::MetaRefreshFire { tab, gen, url }`. El gen counter invalida el refresh si el usuario navegó manualmente antes del timeout o cerró la pestaña. URL `None` recarga la página actual (`refresh` sin `url=`). Status bar muestra `→ refresh en Ns a URL` o `↻ reload en Ns`.
  - Tests nuevos: 6 en engine (`parsea_border_per_side`, `parsea_border_radius_per_corner`, `meta_refresh_extrae_delay_y_url`, `meta_refresh_solo_delay`, `meta_refresh_url_con_comillas`, `meta_refresh_inexistente_devuelve_none`). 188/188 verde (165 engine + 15 core + 8 llimphi).
- **Fase 5.1 (sgte+10):** **Sweep "redirects + fragments + tables"** — 3 features que el HTML antiguo y muchas páginas modernas dan por sentado.
  - **URL final post-redirect propagada**: `fetch` y `post_form` ahora devuelven `(bytes/body, final_url)` y exponen `fetch_bytes_with_url`. El agente ureq se construye con `.redirects(10)` (default era 5) para cadenas largas tipo url-shortener → http→https → www→apex. `Engine::load` y `load_post` usan la URL final como `Document.url` para que los hrefs relativos resuelvan contra ella. `Msg::Loaded` lleva el nuevo campo `final_url: String`; si difiere de la solicitada el chrome actualiza `t.url`/`t.addr` y reemplaza el último entry de history (no empuja uno nuevo — back debe saltar a la página *anterior* al request fallido, no a la URL pre-redirect que sólo redirige otra vez). Cache sigue indexado por la URL **solicitada** para que el mismo permalink ocupe siempre el mismo slot.
  - **Fragment navigation (`#id`)**: `resolve_href` ya no descarta fragmentos puros — los resuelve contra la base (`#top` con base `https://x/p` → `https://x/p#top`). `BoxNode.element_id: Option<String>` se popula desde el atributo `id` (vacío y whitespace se descartan). `BoxTree::find_element_y(id)` estima la posición vertical aproximando margin+padding de bloques y `font_size * line_height` de hojas de texto en DFS pre-order — no es layout real (taffy todavía no corrió cuando el click llega), pero ubica al usuario dentro de ±1 línea. Chrome: helper `same_doc_with_fragment(current, target)` detecta cuándo target sólo difiere en el fragment; en `Msg::Navigate`, si es same-doc, el chrome ajusta `scroll_y`, actualiza `t.url`/`t.addr`/`history` y muestra `↟ #frag` (o `(sin id #frag)` si no se encuentra el target) sin recargar.
  - **Tablas mínimas visibles**: UA stylesheet enriquecido — `<td>`/`<th>` ahora con `padding: 4px`, `border: 1px solid #ccc`; `<th>` mantiene su bold y suma `background: #f2f2f2` para destacarlo como header; `<caption>` queda como block centrado con padding. `<colgroup>` y `<col>` pasan a `Display::None` (eran block, derramaban espacio fantasma — su rol en spec es definir width de columnas, que hoy no soportamos). El resto de la maquinaria de tablas (`<tr>` flex-row, `<td>` inline-block) ya estaba en lo verde — este sweep sólo arregla el "look spreadsheet" para tablas sin CSS de autor (RFC docs, Wikipedia raw, etc.).
  - Tests nuevos: 6 en engine (`link_fragmento_se_resuelve_a_base_mas_frag`, `element_id_se_extrae_del_attr`, `ua_table_cells_tienen_border_y_padding`, `ua_colgroup_y_col_ocultos`, `ua_caption_centrado`) + 1 en llimphi (`same_doc_with_fragment_detecta_solo_fragment`); el viejo `link_fragmento_se_ignora` se reemplazó por la versión que verifica la nueva semántica. 182/182 verde (159 engine + 15 core + 8 llimphi).
- **Fase 5.1 (sgte+9):** **Sweep "los grandes que faltaban"** — 8 features mayores en cadena.
  - **Tab/Shift+Tab nav entre inputs**: `Msg::FocusNext`/`FocusPrev` cicla con wrap. Tab cuando hay input focado no escapa a la página.
  - **Checkbox / Radio / Submit**: `InputKind { Checkbox, Radio, Submit }`. `BoxNode.input_checked_initial` desde el atributo `checked`. `TabState.input_checks: Vec<bool>` paralelo a inputs. Msgs `ToggleCheckbox`/`SelectRadio` (mismo grupo `name+form_idx` se desmarca)/`SubmitForm` (reusa build_form_submit_url). Render ☐/☑ + ○/● + botón gris. Form submit incluye checked como `(name, value)` con fallback "on"; no-checked se omite.
  - **POST submit**: `fetch::post_form(url, body)` con `application/x-www-form-urlencoded`. `Engine::load_post`. Chrome `Msg::NavigatePost { url, body }` + `start_load_post`. `build_form_submit_url` ahora inspecciona `form.method` y emite `Msg::Navigate` (GET) o `Msg::NavigatePost` (POST).
  - **Cookies por host**: nuevo módulo `puriy_engine::cookies` con jar static `Mutex<HashMap<host, Vec<Cookie>>>`. `cookie_header(host)` arma `Cookie: …`; `put_set_cookie(host, sc)` parsea la primera key=value del Set-Cookie. fetch/post_form ahora envían cookies del host y guardan Set-Cookie. Sin Domain/Path/Expires/Secure por ahora.
  - **SVG path/polygon/polyline**: nuevos variants `Polyline { points, closed, ... }` y `Path { d, ... }` con enum `PathCmd { MoveTo, LineTo, CubicTo, QuadTo, ClosePath }`. `parse_svg_path` soporta M/L/H/V/C/Q/Z absolutos y relativos; A/S/T saltean un número. Chrome traduce a `kurbo::BezPath` para pintar.
  - **font-family real (FontStack)**: stack llimphi-text → llimphi-ui → engine → chrome cableado. `ComputedStyle.font_family: Option<String>` heredable; parser captura el string CSS crudo (lista + comillas). `parley::FontStack::Source(Cow::Borrowed(...))` se pushea como style property. Nueva API `View::text_aligned_full(..., font_family)`.
  - **srcset + `<picture>`**: `pick_srcset` parsea `url Nx` o `url Nw` y elige el más alto. `<img srcset>` resuelve antes que `src`. `<picture>`: itera `<source srcset>` y usa el primero que sirva. Tests serializados en `cookies` con un `TEST_LOCK` interno (el jar es global, antes flaky con cargo test paralelo).
  - **Select con overlay flotante**: `App::view_overlay(model)` se implementa en puriy y devuelve, cuando algún `<select>` tiene `open=true`, un backdrop fullscreen semitransparente con la lista de opciones centrada por flex. Click en el backdrop cierra; click en opción dispara `SelectPick`. `render_select` ya no expande inline.
  - Tests nuevos (8): cookies x3, srcset x3, svg polygon/path x2. 177/177 verde (155 engine + 15 core + 7 llimphi).
- **Fase 5.1 (sgte+8):** **Sweep "todos los que faltaban"** — 6 features mayores en cadena.
  - **`:focus` pseudoclase**: `Pseudo::Focus` (alias `:focus`/`:focus-visible`/`:focus-within`). Nueva `StyleEngine::compute_with_parent_for_state(..., hover, focus)`. `BoxNode.focus_background` precomputado igual que hover_background. Chrome aplica focus_background al input focado; si no hay outline CSS, dibuja un ring azul accent. Enter en input deja un hint hasta que el form submit lo intercepta (siguiente entry).
  - **`font-style: italic`**: stack completo. `llimphi-text::TextBlock.italic` pushea `parley::StyleProperty::FontStyle(Italic)`. `llimphi-ui::View::text_aligned_italic`. Engine: `FontStyle { Normal, Italic }` (heredable) + parser (`italic`/`oblique[…]`). UA defaults para `em`/`i`/`cite`/`dfn`/`var`/`address`/`blockquote`. Chrome usa el italic flag por leaf de texto.
  - **Hover URL preview**: `llimphi-ui::View::on_pointer_enter(msg)`/`on_pointer_leave(msg)`. El runtime dispatchea las dos transiciones cuando el hover_idx cambia. Puriy expone `Msg::HoverLink(Option<String>)` y reemplaza la status bar por `→ <url>` mientras el cursor está sobre un link.
  - **Form submit (GET)**: `BoxTree.forms: Vec<FormInfo { action, method }>` + `BoxNode.form_idx`. Post-pass `assign_form_idx` con stack DFS. Enter en input dispara `build_form_submit_url` que junta `(name, value)` de inputs y selects del mismo form, URL-encodea mínimamente y dispatcha `Msg::Navigate(action?n1=v1&...)`. POST no implementado todavía.
  - **`<select>`/`<option>`**: `SelectInfo { options, initial }` recolectado en build_node. UA `option`/`optgroup` → `Display::None`. Chrome: `TabState.selects: Vec<SelectState { selected, open }>` + msgs `SelectToggle`/`SelectPick`. `render_select` arma header con flecha ▼/▲ y lista expandida inline al abrir. Submit incluye el value del select por su `name`. Sin overlay flotante / búsqueda / Tab nav.
  - **SVG mínimo**: `SvgScene { width, height, view_box, prims }` + `SvgPrim { Rect, Circle, Line }`. `<svg>` pasa de `Display::None` a `InlineBlock`. `<g>` se atraviesa transparente. Chrome `render_svg` usa `paint_with` para pintar las primitivas con mapping viewBox→rect preservando aspect ratio. `<path>`/`<polygon>`/`<text>` ignorados.
  - Tests nuevos: 8 en engine (`focus_pseudo` x2, italic x2, form, select, svg, ua_svg_inline). 169/169 verde (147 engine + 15 core + 7 llimphi).
- **Fase 5.1 (sgte+7):** **Sweep "hazles todas"** — 8 features visibles encadenados.
  - **Match navigation en Find** (Enter/Shift+Enter): `Model.find_current: usize` (1-based, 0 = sin nav). Enter avanza con wrap, Shift+Enter retrocede. Tipear en input resetea current a 0. Find bar muestra `X de N` cuando se navegó, sino `N matches · Enter`. Match "actual" pinta naranja (#FF8C00 α240); otros amarillo.
  - **Filtro en paneles Bookmarks/History**: `Model.panel_filter: TextInputState`. Substring case-insensitive contra title+url. Mientras el panel está abierto, todas las teclas (excepto Esc/F5) van al filtro vía `Msg::PanelFilterKey`. Toggle del panel limpia el filtro.
  - **Page source view** (Ctrl+U): `Document.source: String` en engine; `TabState.source` en chrome. Nueva `PanelKind::Source` con header dark + lista de líneas numeradas (4 dígitos derecha, gris), cap 2000 líneas. Mismo Esc/✕ que los otros paneles.
  - **img max-width 100% + aspect-ratio**: `image_view` setea `aspect_ratio = Some(w/h)` para que taffy preserve proporción cuando el ancho real es menor que el nativo. Imágenes grandes ya no rompen layouts narrow.
  - **background-image: url(...)**: parser `parse_background_image` acepta `url('…')`/`url("…")`/`url(…)`. `ComputedStyle.background_image_url`, `BoxNode.background_image: Option<ImageData>` resuelto y descargado en build_node (misma cache que `<img>`). Chrome lo pinta dentro de `apply_decorations` vía `scene.draw_image` con scale "cover" anclada arriba-izquierda. Sin `background-size`/`-position` por ahora.
  - **Hover background en summary**: el row flecha + summary lleva `hover_fill(rgba 0,0,0,18)`. Click handler sube al row entero.
  - **Link hardening + target=_blank**: `resolve_href` rechaza `mailto:`/`tel:`/`sms:`/`data:`/`javascript:`/`ftp:`/`file:` y fragmentos puros (`#anchor`). Sólo `http`/`https`/`about` siguen siendo navegables. `BoxNode.link_new_tab: bool` setea desde `<a target=_blank>` (o cualquier target no-self); click dispara `Msg::NavigateNewTab(url)` que abre pestaña nueva.
  - **Inputs/textarea interactivos**: `InputKind { Text, Password, Search, TextArea }`. Engine extrae el value/contenido inicial y el placeholder. Chrome lleva `TabState.inputs: Vec<TextInputState>` + `focused_input: Option<usize>`. Click en input dispara `Msg::FocusInput(idx)`; mientras está focado, las teclas van al `text_input_view` vía `Msg::InputKey`. Esc blurea. **Refactor**: `RenderCtx<'a>` agrupa zoom + find_* + details_* + inputs + focused_input para que `render_box`/`render_link_subtree` no tengan 10 params; los counters viven adentro del ctx.
  - Tests nuevos: 8 en engine (`details_*` x4, `link_*` x3, `parsea_background_image_url`, `background_image_none_limpia_url`, `ws_*` ya existían). 162/162 verde (140 engine + 15 core + 7 llimphi).
- **Fase 5.1 (sgte+6):** **Strip de whitespace inter-block** (fix grueso del rendering de páginas sin CSS). El HTML `<body>\n  <p>...</p>\n  <h2>...</h2>\n</body>` deja en el DOM Text nodes con sólo `"\n  "` (que el collapse colapsa a `" "`). Como cada hoja de texto se materializa con `height = font_size * line_height ≈ 22.4px`, esos espacios convertían el body de motherfuckingwebsite en una columna llena de gaps fantasma (95 boxes contra los 66 reales). Browsers reales eliminan esos text nodes whitespace-only que separan block siblings o quedan adyacentes al borde de un bloque. Implementado en `strip_block_adjacent_whitespace(children, parent_display)` invocado al final de `build_node` (rama Element y rama Document). El filtro NO se aplica si el padre es `Display::Inline` (ej: `<span> </span>` debe preservar su espacio porque vive en el inline-flow del abuelo block); sí se aplica para Block/Flex/Grid/InlineBlock/etc. Drop iff (vecino prev es block-level o ausente) Y (vecino next es block-level o ausente). motherfuckingwebsite.com: 95→66 boxes (-30%). Tests nuevos: `ws_entre_blocks_se_filtra`, `ws_alrededor_de_inline_se_preserva`, `ws_solo_inline_no_se_dropea_si_padre_es_inline_flow`. 157/157 verde (135 engine + 15 core + 7 llimphi).
- **Fase 5.1 (sgte+5):** **`<details>`/`<summary>` interactivos** — toggle real (open/closed) con flecha clickeable ▶/▼ precediendo al summary. Engine: `BoxNode.details_open_attr: bool` se setea en `build_node` consultando `dom::attr(node, "open").is_some()` para nodos con tag `<details>`. Chrome: `TabState.details_open: Vec<bool>`, prefilleado en `Msg::Loaded` walkeando el box tree en DFS y empujando `b.details_open_attr` por cada `<details>`. El render walk hilvana `details_counter: &mut usize` por todo el árbol; al toparse con un `<details>`, reserva su slot, decide open/closed, y renderea sólo el `<summary>` (precedido de flecha ▶/▼ clickeable) si está cerrado. Click en flecha o en summary dispara `Msg::ToggleDetails(idx)` que flippea `details_open[idx]`. **Gotcha clave**: si un `<details>` está cerrado, NO renderea los non-summary children pero igual avanza el counter por cada `<details>` anidado adentro (vía `skip_count_details`) — sin esto, los índices se desalinearían respecto al vector que el Loaded prefilló en DFS completo. Tests nuevos: 4 en engine (`details_sin_open_attr_arranca_cerrado`, `details_con_open_attr_lo_refleja`, `details_summary_se_parsean_como_tags`, `details_open_attr_es_false_para_nodos_no_details`) + 2 en llimphi (`skip_count_details_avanza_por_cada_details_anidado`, `skip_count_details_no_cuenta_otros_tags`). 154/154 verde (132 engine + 15 core + 7 llimphi).
- **Fase 5.1 (sgte+4):** **Panel de Bookmarks / Historial** (`Ctrl+B` toggle bookmarks, `Ctrl+H` toggle historial, `Esc` cierra, click ✕ del header también). `Model.panel: Option<PanelKind>` (`Bookmarks | History`). Cuando abierto, el panel reemplaza el viewport (header con título + count + ✕, y lista de items debajo); el resto del chrome (tabs / address bar / status) sigue visible. Cada item muestra `title` arriba y `url` abajo (gris, más chico), click en cualquier parte del item dispara `Msg::Navigate(url)` que además cierra el panel; en bookmarks aparece un botón ✕ a la derecha que dispara `Msg::RemoveBookmark(id)` y persiste el profile. Historial se ordena descendente (más reciente primero) y no tiene botón de borrado individual por ahora. Si el chrome corre sin profile (`run(url)` en vez de `run_with_profile`), el panel muestra `(sin profile)` en el título. Estado vacío con tooltip educativo (`(no hay bookmarks · Ctrl+D guarda la pestaña activa)` / `(historial vacío)`). 148/148 verde (sin tests nuevos — la cobertura del Profile la dan `puriy-core`).
- **Fase 5.1 (sgte+3):** **Find in page** (`Ctrl+F` abre, `Esc` cierra). `Model.find_active: bool` + `find_input: TextInputState`. Cuando está activa, una find bar (background oscuro, input + contador + close) se inserta entre el header y el viewport; `on_key` redirige todas las teclas al input excepto `Esc`. Highlight uniforme en amarillo (`#FFE600`, alpha 200) a cada hoja de texto cuyo `text.to_lowercase()` contiene la query (también lowercased una vez fuera del walk). Contador `N matches` / `1 match` / `sin matches` / `(escribí algo)` en la barra. Por ahora sin "match X of N" ni auto-scroll al match — el usuario navega manualmente con la wheel/PgDn/etc. Reabrir la find bar limpia la query previa. 5 tests nuevos sobre `count_matches`: vacíos/None/case-insensitive/hits/no-hits. 148/148 verde (128 engine + 15 core + 5 llimphi).
- **Fase 5.1 (sgte+2):** **Page zoom** (`Ctrl+=` / `Ctrl++` / `Ctrl+-` / `Ctrl+0`). `Model.zoom: f32` (default 1.0, clamp 0.5..3.0, step ×/÷ 1.1) escala todo el render del box tree: font-size del texto, padding/margin Sides, gap, line-height, insets Px (los Pct quedan intactos porque resuelven contra el contenedor que también escala), grid tracks Px, image dims, border width, border-radius, outline width+offset, box-shadow offset/blur/spread, text-decoration thickness y posición. El marco interno del viewport (24/16 px) y el chrome (toolbar/tabs/status) NO se escalan — sólo el contenido. Status bar muestra `zoom: N%` cuando ≠ 100%. Atajos parseados desde `Key::Character("=")`/`("+")`/`("-")`/`("_")`/`("0")` con `mods.ctrl` para tolerar layouts sin numpad. 143/143 verde (128 engine + 15 core).
- **Fase 5.1 (sgte+1):** `list-style-type` — numerales y bullets reales. (a) **Engine**: `ComputedStyle.list_style_type: ListStyleType` (None/Disc/Circle/Square/Decimal/LowerAlpha/UpperAlpha/LowerRoman/UpperRoman). Heredable según CSS spec — el `<li>` recoge el valor del `<ol>`/`<ul>` padre. Parser de `list-style-type` y del shorthand `list-style` (sólo se captura el `-type`; `-position`/`-image` se ignoran). Aliases `lower-latin`/`upper-latin` aceptados. UA stylesheet: `<ol>` → decimal, `<ul>`/`<menu>` → disc. (b) **Boxes**: el viejo `"• "` hardcoded en `build_node` se reemplaza por `li_marker(node, list_style_type)`. Para tipos numerados, `ol_item_position` cuenta los `<li>` hermanos respetando `<ol start="N">` y `<li value="N">` (ambos modifican el contador). Romanos vía tabla subtractiva (1..3999, fallback decimal fuera de rango), alfa via bijective base-26 (`27 = aa`). Símbolos disc/circle/square usan Unicode (`•`/`◦`/`▪`). `list-style-type: none` suprime el marker entero. (c) **Tests nuevos** (11): `parsea_list_style_type`, `parsea_list_style_shorthand`, `ua_aplica_decimal_a_ol_y_disc_a_ul`, `list_style_type_hereda_de_padre_a_li`, `ol_li_recibe_marker_decimal`, `ul_li_recibe_marker_bullet`, `list_style_none_suprime_marker`, `ol_start_corre_el_contador`, `li_value_resetea_el_contador`, `lower_roman_y_lower_alpha_aplican`, `to_alpha_y_to_roman_son_correctos`. 74 engine + 15 core = 89/89 verde.

## Relacionados

- [[project-llimphi]] — la pila gráfica que puriy consume
- [[project-mirada]] — compositor Wayland donde puriy abre ventana en Linux
- [[project-wawa]] — kernel SASOS donde puriy abre framebuffer bare-metal
- [[project-pluma]] — visor markdown hermano (ambos en 00_unanchay, ambos visualizadores)
