[package]
name = "agora-gossip"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true
description = "agora — protocolo de gossip transport-agnóstico: dos nodos convergen sus TrustGraphs intercambiando digests de hashes + bundles. Sin IO; el transporte (AoE, TCP, sneakernet) lo decide el caller."

[dependencies]
agora-core = { path = "../agora-core" }
agora-graph = { path = "../agora-graph" }
serde = { workspace = true }
