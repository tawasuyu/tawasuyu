# agora-graph

> Thread graph + relations of [agora](../README.md).

DAG of messages with `Reply`, `Quote`, `Crossref` relations. Useful for "thread", "conversation", "mentions" views.

## Deps

- [`agora-core`](../agora-core/README.md), `petgraph`
