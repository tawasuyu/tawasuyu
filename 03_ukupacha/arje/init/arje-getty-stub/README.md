# arje-getty-stub

> Minimal login for [arje](../../README.md).

Waits on TTY, reads user + password, validates against the local system DB, launches shell. No PAM — direct auth. Useful for small or rescue systems.

## Deps

- `nix`
