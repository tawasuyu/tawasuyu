# arje-getty-stub

> Login mínimo para [arje](../../README.md).

Espera en TTY, lee usuario + clave, valida contra la DB local del sistema, lanza shell. Sin PAM — autenticación directa. Útil para sistemas chicos o de rescate.

## Deps

- `nix`
