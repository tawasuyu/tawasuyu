# arje-echo

> Early logging for [arje](../../README.md).

Output to serial/console before `arje-bus` is ready. In-memory buffer; flushes to the journal when the bus comes up. Critical for boot debugging.

## Deps

- `nix`
