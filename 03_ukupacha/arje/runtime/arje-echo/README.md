# arje-echo

> Logging temprano para [arje](../../README.md).

Output a serial/console antes de que `arje-bus` esté listo. Buffer en memoria; vuelca al journal cuando el bus arranca. Crítico para debug de boot.

## Deps

- `nix`
