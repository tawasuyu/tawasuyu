target/
Cargo.lock
*.bak
*.bak2
.DS_Store
*.swp
*~
node_modules/
dist/
*.log

# Artefactos wasm-bindgen de la landing (se regeneran con build-tawasuyu-web.sh)
pkg/
# Salida accidental de build fuera de árbol (no es un dominio del workspace)
/crates/

# Artefacto de deploy de la landing (build-tawasuyu-web.sh con DEST); el sitio se sirve del repo vivo
/release/

# Frames intermedios del showreel (regenerables con scripts/showreel.sh)
showreel_frames*/
