[package]
name = "hapiy-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true
description = "hapiy — núcleo agnóstico de captura de pantalla: Shot/Region, recorte, encode PNG, destino (archivo / editar en tullpu) y el trait Capturer. Sin Wayland ni UI."

[dependencies]
image = { workspace = true }
directories = { workspace = true }
# Sólo el esquema (no_std) del centro de eventos, para construir el Evento de una
# captura. El envío (willay-emit, socket) lo hacen los binarios, no el núcleo.
willay-core = { workspace = true }
