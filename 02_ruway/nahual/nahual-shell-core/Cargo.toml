[package]
name = "nahual-shell-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true
description = "nahual-shell-core — lógica agnóstica del shell nahual: motor de búsqueda (find léxico por nombre/contenido + semántico por embeddings), glob-match y los tipos de resultado. Cero UI, cero Llimphi; cualquier frontend (Llimphi, CLI, web) lo consume."

[dependencies]
# Búsqueda semántica: embeddings vía el daemon de verbo. Degrada a búsqueda por
# nombre si el daemon no está corriendo.
rimay-verbo = { path = "../../../00_unanchay/rimay/rimay-verbo" }
tokio = { workspace = true }
# Ejecución de las operaciones de archivo (`ops`) por la cara SourceMut. ops es
# POSIX-only: usa PosixSource, no necesita los backends nouser/minga del frontend.
nahual-source-core = { path = "../nahual-source-core" }

[dev-dependencies]
tempfile = { workspace = true }
