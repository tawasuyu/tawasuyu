# supay-scene

> Level snapshot of [supay](../README.md): sectors, mobjs, player.

Level state reconstruction from the engine: list of sectors with their pic_idx, mobjs with sprite + position + angle, player with camera. No gameplay data — only what's needed to render.

## Deps

- `serde`, `glam`
