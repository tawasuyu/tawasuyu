# Tullpu — editor de imágenes por capas, IA-able

> Tullpu (quechua: *teñir, dar color, pigmento*). Tipo: **Layered image editor over Llimphi**, edición no destructiva con operaciones IA como nodos del DAG.

> Estado: **propuesta de diseño** (2026-05-29). Nada en disco todavía salvo este SDD. Referenciado en `PLAN.md` §6.ter (tabla office/PSD) y §6.quinquies (multimedia).

## Tesis

Un editor de imágenes donde **la pila de capas ES un DAG content-addressed** y **cada operación (filtro, ajuste, op IA) es un nodo derivado**, no un píxel pisado. Edición no destructiva por construcción: deshacer es navegar el grafo, no un buffer de undo. La parte "IA-able" no acopla tullpu a ningún modelo — las ops IA hablan con un daemon de modelos de píxel por socket (calco de `rimay-verbo-daemon`).

El insight central: **es el mismo patrón que `pluma` ya inventó para texto** (haz de cuerpos, ver `PLAN.md` §11). En pluma, una traducción/resumen es un *cuerpo hijo* derivado de un *cuerpo madre* por una `Transformacion`; si la madre cambia, la hija queda *stale* y la UI pinta la hebra punteada con botón "regenerar". En tullpu, un inpaint/upscale/segmentación es una *capa hija* derivada de una *capa madre* por la misma maquinaria. Reuso conceptual total, distinta carga útil (píxeles en vez de párrafos).

## Por qué encaja en gioser sin fricción

- **Capas = DAG.** Una pila de capas con sus modos de fusión es literalmente un grafo dirigido. Se mapea sobre `format::Objeto{datos, hijos}` (BLAKE3 + postcard) sin modelo nuevo.
- **Dedup automática.** Dos capas con contenido idéntico (o dos versiones de un proyecto que comparten un fondo) comparten hash y se almacenan una sola vez en `almacen.rs`.
- **No destructivo gratis.** Cada ajuste es un nodo. Cambiar un parámetro aguas arriba marca *stale* el cono descendiente; la UI lo regenera bajo demanda.
- **UI = frontend intercambiable.** `tullpu-core` no sabe quién lo pinta. El frontend Llimphi se monta sobre `llimphi-widget-nodegraph` (ya pensado para tullpu, ver su doc) + un lienzo de pintura custom.
- **Formatos ajenos por puente.** PSD entra por `shared/foreign-psd`, nunca al núcleo. La app trabaja siempre en formato nativo.

## Anatomía — crates

```
[ CUADRANTE III · 0x02 RUWAY — HACER ]

4. tullpu-app-llimphi   — Binario lanzable (en mirada o en wawa)
   │                      Lienzo + panel de capas + grafo de ops + inspector
   │                      Sobre llimphi-widget-nodegraph + color-canvas
   ▼
3. tullpu-render        — Compositor: recorre el DAG top-down, fusiona capas
   │                      (modos de blend) → buffer Rgba8 → llimphi-surface
   │                      Acelerable por compute shader (vello/wgpu) a futuro
   ▼
2. tullpu-ops           — Catálogo de operaciones (nodos del DAG)
   │                      Locales: brush, ajustes (curvas, niveles, HSL),
   │                      filtros (blur, sharpen), máscaras, transformaciones.
   │                      IA (vía pixel-verbo-daemon): segmentar, inpaint,
   │                      upscale, restyle, generar. Cada op = TransformacionPixel
   ▼
1. tullpu-core          — Modelo agnóstico
   │                      Capa, Lienzo, GrafoDeCapas, modos de fusión,
   │                      estado stale/fresh, serialización a Objeto BLAKE3
   │                      Sin deps de Llimphi ni de modelos IA
   ▼
[ Estado puro · DAG content-addressed ]
```

Más:
- `shared/foreign-psd` — puente psd ↔ grafo de capas tullpu (import; export lossy a futuro).
- `pixel-verbo-daemon` — sirve modelos de imagen (ONNX) por socket Unix; consumidores cambian `Mock` por `DaemonClient::connect(...)`. Ver `PLAN.md` §6.quinquies.

## Modelo de datos (borrador)

```rust
// tullpu-core
struct Capa {
    id: Uuid,                 // estable a través de regeneraciones
    contenido: Hash,          // BLAKE3 del buffer Rgba8 (Objeto en el grafo)
    blend: ModoFusion,        // Normal, Multiplicar, Pantalla, Superponer, ...
    opacidad: f32,
    mascara: Option<Hash>,    // máscara alfa, otro Objeto
    visible: bool,
    origen: OrigenCapa,       // Raster | Derivada(TransformacionPixel)
}

enum OrigenCapa {
    Raster,                                  // pintada/importada a mano
    Derivada { madre: Uuid, op: TransformacionPixel, estado: Frescura },
}

enum Frescura { Fresca, Stale }              // stale ⇒ la UI ofrece "regenerar"

enum TransformacionPixel {
    Local(OpLocal),                          // determinista, en proceso
    Ia { modelo: String, prompt: Option<String>, params: Value },  // vía daemon
}
```

Una capa derivada que queda *stale* (su madre cambió) pinta la conexión punteada en el grafo — idéntico al *stale* de `pluma-notebook` y al haz de cuerpos. La frescura se propaga por el cono descendiente.

## Decisiones abiertas

- **Compositing CPU vs GPU.** Arranca en CPU (`image` crate ya en el stack); migrar a compute shader vía wgpu cuando el lienzo lo pida. `tullpu-render` aísla la decisión.
- **Resolución de escala.** Buffers grandes no caben cómodos en un solo `Objeto`; evaluar tiling (capa = grafo de tiles content-addressed → dedup por tile, ideal para fondos repetidos).
- **Export.** Reusar `pineal-export` (ya hace PNG/SVG/GIF) para la salida; PSD de salida es post-MVP.

## Fases de forja

1. **`tullpu-core`** ✓ — Capa, GrafoDeCapas, modos de fusión, serialización a `Objeto`. Testeable con `cargo test` sin gráficos.
2. **`tullpu-render`** ✓ — compositor top-down CPU → buffer Rgba8. Hito: cargar 3 capas PNG y componerlas a un PNG de salida correcto.
3. **`tullpu-app-llimphi`** ✓ (MVP) — lienzo + panel de capas + paleta. Botón clicable por capa; lienzo central pinta `peniko::Image`. Pendiente: nodegraph sobre `llimphi-widget-nodegraph` cuando llegue `llimphi-surface`.
4. **`tullpu-ops` locales** ✓ — invertir/brillo/contraste/niveles/blur/opacidad/saturación/tonalidad como nodos derivados con *stale tracking*.
5. **`pixel-verbo-daemon` + `tullpu-ops` IA** ✓ — `pixel-verbo-{core,mock,daemon,daemon-bin}`; `regenerar_stale_con_ia` cablea `TransformacionPixel::Ia` al `Proveedor`. Mock determinista (segmentar/inpaint/restyle/generar). Daemon `std::thread`-por-conexión, cliente bloqueante. La app resuelve daemon→mock al arranque y lo muestra en el header. Pendiente: proveedor ONNX real (segment-anything, restyle, upscale), tiling para imágenes grandes.
6. **`shared/foreign-psd`** ✓ — `importar_psd(bytes) -> DocumentoPsdImportado{lienzo, buffers, informe}`. Una capa PSD → una `Capa` raster con buffer Rgba8 hasheado BLAKE3 (dedup automática). Blend modes mapeados al catálogo de `ModoFusion`; los no soportados (Soft Light, HSL, Burn…) caen a `Normal` y se anotan en el informe. Sin máscaras / grupos / clipping / ajustes (post-MVP). Ejemplo `psd_a_png` cierra el loop: PSD real → tullpu-render → PNG.
7. **Blend modes faltantes en `tullpu-render`** ✓ — `ModoFusion` extendido con 12 variantes Photoshop por-canal: `SubExpQuemado` (Color Burn), `SubLinealQuemado` (Linear Burn), `SobreExpAclarado` (Color Dodge), `LuzFuerte` (Hard Light), `LuzSuave` (Soft Light, fórmula W3C), `LuzViva` (Vivid Light), `LuzLineal` (Linear Light), `LuzPunto` (Pin Light), `MezclaDura` (Hard Mix), `Exclusion`, `Resta` (Subtract), `Division` (Divide). `mezclar_canal` cubre cada uno con clamps explícitos para evitar NaN en bordes (`src=0` en burn, `src=1` en dodge). `foreign-psd::mapear_blend` ahora mapea esos 12 discriminantes PSD directo (antes caían a Normal con `degradado=true`). App: `siguiente_blend` y `etiqueta_blend` cubren las nuevas variantes; el ciclador del botón "blnd" recorre las 20.
8. **Blends HSL (no-separables)** ✓ — `ModoFusion` cierra la familia con `HslTono` (Hue), `HslSaturacion` (Saturation), `HslColor` (Color), `HslLuminosidad` (Luminosity). Implementación via W3C Compositing & Blending L1 §10.3: luminosidad ponderada `Lum = 0.3R + 0.59G + 0.11B`, `Sat = max−min`, `SetLum` con `ClipColor` (proyecta al cubo `[0,1]³` preservando el matiz), `SetSat` por reordenamiento de canales. `mezclar_canal` cortocircuita antes del despacho per-channel — los HSL operan sobre el triple, no por canal. `foreign-psd::mapear_blend` ahora cablea los 4 discriminantes HSL directo. Tests cubren: matiz preservado al colorizar grayscale, luminosidad pasada desde src (blanco/negro), saturación cero anula a gris uniforme, lum de dst preservada al cambiar tono.
9. **Comparativos por luminosidad** ✓ — `ModoFusion::ColorMasOscuro` (Photoshop "Darker Color") y `ColorMasClaro` ("Lighter Color"). No factorizan por canal: la decisión es por píxel completo — `Lum(src) ⋚ Lum(dst)` selecciona el triple ganador y se devuelve entero. `mezclar_canal` los cortocircuita junto a los HSL antes del despacho per-channel; convención de empate: gana `dst`. `foreign-psd::mapear_blend` cablea los discriminantes 7 (DARKER_COLOR) y 12 (LIGHTER_COLOR) directo. App: el ciclador `blnd` ahora recorre 22 modos.
10. **Dissolve — catálogo Photoshop cerrado** ✓ — `ModoFusion::Disolver` umbraliza el alfa efectivo (`sa * opacidad * mascara`) contra un ruido PRNG estable por píxel; si gana el alfa, el píxel sale 100% src opaco, si no, el dst se mantiene. Semilla: primeros 8 bytes del `Capa.id` (Uuid es estable a través de regeneraciones, así que el patrón acompaña a la capa aunque cambie su contenido). PRNG: splitmix64 mezclando `seed + i * 0x9E3779B97F4A7C15` (golden ratio), mantissa de 24 bits para evitar artefactos en el borde 1.0. Vive en rama propia `fundir_disolver` dentro de `fundir_capa` — no encaja en `mezclar_canal` porque la decisión es per-píxel binaria sobre el alfa, no una mezcla `(s,d)`. `foreign-psd::mapear_blend` ahora cablea DISSOLVE directo: **el catálogo Photoshop completo mapea sin degradado** (los 28 discriminantes upstream cubiertos). Tests cubren: alfa=1 pinta todo src; alfa=0 deja todo dst; alfa=0.5 da ~50% mix (tolerancia ±10% en 4096 píxeles); render es determinista (a==b bit a bit entre composiciones del mismo lienzo); el patrón cambia con `Capa.id` distinto. App: ciclador a 23 modos.
11. **Export — guardar lienzo como PNG** ✓ — `tullpu-render::exportar_png(lienzo, fuente, ruta) -> Result<RgbaImage, Error>` envuelve `componer` + `RgbaImage::save`; el formato lo deduce `image` por extensión (PNG, JPEG, etc.). Nuevo variant `Error::Imagen(image::ImageError)` propaga fallas de codificación; si `componer` falla antes, el archivo no se crea. App: variante `Msg::ExportarPng` + botón `💾 exportar PNG` arriba en el panel ops (sección "salida", sin lock por selección — exportar es global). Path generado: `tullpu-export-<unix_ts>.png` en CWD (sin file picker todavía); el header muestra el path final o el error. Tests: roundtrip (lienzo 4×3 con dos capas → PNG → re-decode → píxeles idénticos al compuesto en RAM), y propagación de error (si compose falla, el archivo no se toca).
12. **Cargar PSD desde la app** ✓ — la app cierra el loop "PSD real → editar → exportar PNG". `cargar_arg(path)` despacha por extensión: `.psd` cae a `cargar_psd` (que llama `foreign_psd::importar_psd`, vuelca los buffers únicos al `AlmacenEnMemoria` y devuelve el `Lienzo` multi-capa con el `Uuid` del fondo); cualquier otra extensión cae al loader PNG existente (capa raster única). `inicializar()` usa el helper y `lienzo_default` si no hubo arg o falló la carga. El header muestra una etiqueta tipo `psd · 12 capas` o `psd · 12 capas (2 a Normal)` cuando hay degradados — tras Fase 10 el degradado sólo aparece si Adobe agrega blends nuevos al spec.
13. **Export multiformato (PNG/JPEG/WebP)** ✓ — `FormatoExport::{Png, Jpeg{calidad: u8}, Webp}` + `tullpu_render::exportar(lienzo, fuente, ruta, formato)`. PNG y WebP usan `image::codecs::*::*Encoder` directos sobre Rgba8 (lossless, conservan alfa); WebP es lossless puro-Rust (el feature `webp` se agregó al workspace). JPEG descarta alfa (encoder no la acepta), convierte a Rgb8 vía `DynamicImage::to_rgb8` y usa `JpegEncoder::new_with_quality` con `calidad` clamped a `1..=100`. `exportar_png` queda como wrapper compat. Error nuevo `Error::Io` para `File::create`. App: tres botones independientes (PNG / JPEG q90 / WebP) en la sección "salida"; `extension_export` mapea formato → extensión (`png`/`jpg`/`webp`) para el path con timestamp. Tests: WebP roundtrip bit-a-bit (pixels y alfa idénticos al compuesto), JPEG roundtrip con tolerancia ±6 por canal sobre color sólido a q90, JPEG con `calidad=0` no panickea (clamped a 1, decodable).
14. **Jerarquías de grupos PSD aplanadas** ✓ — el bridge `foreign-psd` recorre `psd.groups()` y, para cada capa, resuelve la cadena de grupos por encima vía `parent_id` (memoizada por `id` en un `HashMap<u32, CadenaGrupo>` — un grupo se resuelve a lo sumo una vez). Las propiedades del grupo se **propagan a la capa hoja**: el `nombre` lleva la ruta jerárquica (`"raíz/sub/hoja"`), la `opacidad` es el producto (clamped a `[0,1]`), la `visibilidad` es AND lógico. El blend de la capa se preserva tal cual. `InformeImportacion` ganó dos campos: `grupos_detectados: usize` y `grupos_con_blend_propio: Vec<(ruta, blend)>` (en Fase 15 renombrado a `grupos_rasterizados`). Tres fixtures nuevos del corpus upstream (`group-one-layer`, `groups-nested`, `groups-siblings`).
15. **Rasterización de grupos PSD non-Normal** ✓ — `foreign-psd` se refactorizó con un árbol explícito `NodoPsd::{Capa{idx}, Grupo{id, hijos}}` reconstruido en `construir_arbol(layers, grupos)` (orden entre hermanos inferido del menor índice de hoja en su descendencia, recursivo y memoizado). `procesar_nodo` recorre en post-order: grupos `Normal`/`PassThrough` se aplanan (mismo comportamiento de Fase 14: prefix path + multiplicar opacidad + AND visible); grupos con blend distinto se **rasterizan** vía `rasterizar_grupo` — un sub-`Lienzo` con sus capas se compone con `tullpu_render::componer` a un único Rgba8 hasheado, y el grupo entero queda como una sola `Capa` raster con el blend, opacidad y visibilidad propios del grupo. `VistaBuffers<'a>` adapta `&HashMap<Hash, Vec<u8>>` al trait `FuenteBuffers` sin copias. `foreign-psd` agrega dep en `tullpu-render`. Renombre: `grupos_con_blend_propio` → `grupos_rasterizados`. Tests nuevos (3, total 14 verde) contra el helper directamente (no hay fixture PSD non-Normal en el corpus).
16. **Thumbnails por capa en el panel** ✓ — el panel de capas pinta un mini-render de 22×22 px a la izquierda del nombre de cada fila. `Model.thumbs: HashMap<Hash, peniko::Image>` cachea por hash del buffer Rgba8 — dos capas con el mismo contenido comparten el thumb sin re-renderizar. `sincronizar_thumbs(&mut model)` corre tras `inicializar()` y tras `aplicar_y_recomponer`: hace GC de entries cuyos hashes ya no aparecen en el lienzo (las derivadas cambian de hash al regenerar) y genera los que falten. `thumbnail_de_buffer` usa `image::imageops::thumbnail` (Catmull-Rom, suficiente para 22 px) sobre el Rgba8 del buffer y envuelve en `peniko::Image`. La fila pinta un slot fijo de 24×24 aunque el cache no tenga el thumb listo (fill `bg_panel_alt`) — evita reflow en el primer frame.
17. **Reordenar y duplicar capas desde el panel** ✓ — `tullpu-core::Lienzo` gana `mover_arriba(id)` / `mover_abajo(id)` (swap con el vecino, no-op en bordes; devuelven `bool`) y `duplicar(id) -> Option<Uuid>` (inserta clon con `Uuid` nuevo justo encima de la original; nombre con sufijo `" (copia)"`; raster comparte el mismo hash de contenido —el almacén ya no replica buffers—; derivada preserva madre/op/frescura/contenido_cache, así la duplicación no fuerza regeneración si la original era Fresca; las hijas existentes siguen apuntando a la original). Las dependencias madre→hija son por `Uuid`, no por índice, así que reordenar nunca rompe el grafo. App: `Msg::{MoverArriba, MoverAbajo, Duplicar}` + tres mini-botones nuevos (`↑` `↓` `⎘`) en cada fila del panel de capas, a la derecha de `blnd`. Semántica visual: como la lista pinta top→fondo, `↑` mueve la capa visualmente hacia arriba en la lista (= índice mayor en `capas` = más cerca del tope de composición), coincidiendo con `Lienzo::mover_arriba`. Tests nuevos en `tullpu-core` (2, total 10 verde): reorden con casos borde + capa inexistente; duplicar inserta en `i+1`, preserva contenido y mapeo de madre.
18. **Agregar capa desde archivo — fuzzy file picker** ✓ — la app cierra el loop "compositing real entre fuentes distintas": hasta acá un lienzo se llenaba con lo que llegara por CLI. Ahora `tullpu-app-llimphi` walkea CWD al arrancar (`walk_imagenes` — recursivo, exclude dotfiles/`target`/`node_modules`/`pkg`, cap 50k, filtro `.png`/`.jpg`/`.jpeg`), guarda los paths en `Model.imagenes_disponibles` y monta `llimphi-module-file-picker` como overlay vía `App::view_overlay`. `App::on_key` rutea: si el picker está abierto, sus teclas van al módulo; si no, `picker::open_shortcut` (Ctrl+P) lo abre. Botón "📂 capa desde archivo · N candidatos · Ctrl+P" en una sección nueva "entrada" del panel ops, arriba de "salida". Cuando el picker emite `PickerAction::Open(path)`, `agregar_capa_desde_archivo` decodea PNG/JPEG, llama `ajustar_a_lienzo` (fit-contain Lanczos3 + pad transparente centrado si las dims difieren del lienzo — la capa "asoma" en vez de deformarse), `almacen.insertar` dedup el buffer por hash, y la `Capa::raster` nueva se inserta justo encima de la seleccionada (no al tope a ciegas) preservando el contrato top→fondo del panel. Tests del binario (4, todos verde): `ajustar_dims_iguales_devuelve_sin_tocar`, `ajustar_contain_pad_transparente_centrado` (verifica que las esquinas del rect 200×200 quedan transparentes y el centro opaco al meter un fuente 100×50), `ajustar_dst_cero_devuelve_none` (defensivo), `es_imagen_soportada_filtra_extensiones` (case-insensitive, .psd queda fuera — PSD entra todavía sólo por CLI).
19. **Slider de opacidad in-situ** ✓ — los botones `α−` / `α+` (saltos discretos de 0.1) se reemplazaron por un `llimphi-widget-slider` embebido en cada fila de capa. Drag continuo en `[0.0, 1.0]` con feedback numérico inmediato (`+0.50`) a la derecha del track. El widget devuelve `dv` (delta de valor) por cada `DragPhase::Move`; `Msg::BumpOpacidad(id, dv)` ya acumulaba deltas, así que el hook es directo — sólo emitimos en `Move` (`End` no aporta nuevo delta). Paleta compacta nueva (`slider_pal_compacto`): `label_width: 0` (el nombre de la capa ya identifica), `track_width: 56`, `value_width: 36`, `row_height: 24`. El panel de capas pasó de 360 → 400 px de ancho para absorber el extra (slider 92 px vs. dos botones 68 px).
20. **Drag & drop OS-level** ✓ — soltar un archivo desde el file manager directamente sobre la ventana de tullpu lo agrega como capa nueva. Requirió un evento nuevo en `llimphi-ui`: `App::on_file_drop(&model, PathBuf) -> Option<Msg>` con default `None` (backward-compatible — todas las apps existentes siguen compilando sin cambios) cableado al `WindowEvent::DroppedFile(PathBuf)` de winit. La posición del drop no se reporta porque winit no la expone hasta que el compositor la propague (en Wayland depende del extension `data_device_manager`; en X11 viene en el ClientMessage XDND). `tullpu-app-llimphi` overridea con `Msg::FileDrop(path)` que reutiliza exactamente `agregar_capa_desde_archivo` — sin preflight de extensión, si no es PNG/JPEG la decodificación falla y el lienzo queda intacto con el estado descriptivo. PSD por drop queda fuera por la misma razón que en el picker — todavía sólo entra por CLI.
21. **Hotkeys sobre la capa seleccionada** ✓ — `on_key` dispatcha vía `hotkey_a_msg` (función pura, testeable): `Delete`/`Backspace` elimina, `Ctrl+D` duplica, `V` toggle visible, `B`/`Shift+B` cicla blend forward/reverse, `[`/`]` opacidad ∓0.1, `F2` inicia renombrado, `Ctrl+S` exporta PNG, `Ctrl+Shift+S` exporta WebP. Los hotkeys de capa se silencian si no hay selección; los globales (export) funcionan siempre. Cuando el picker está abierto o se está renombrando, los hotkeys quedan suspendidos — las teclas van al modal correspondiente. El ciclo forward/reverse del blend se factorizó: ambos derivan de la misma constante `CICLO_BLEND: &[ModoFusion]` indexando módulo `len()`, evitando dos `match` paralelos que se desincronicen al agregar variantes. Tests del binario (8 nuevos, total 12): `blend_anterior` inverso de `siguiente_blend` (con verificación de rotación completa = identidad), un test por cada hotkey, y dispatch sin selección — Ctrl+S funciona, todo lo demás devuelve `None`.
22. **Renombrar capas in-situ (F2)** ✓ — `Model.renombrando: Option<(Uuid, TextInputState)>` arranca pre-cargado con el nombre actual de la capa (editar es "tocar el final" en vez de "borrar todo y tipear"). Mientras está activo, `on_key` route todas las teclas al `TextInputState` (`TeclaRenombrar(ev)`) excepto `Enter` (`ConfirmarRenombrar` — actualiza `Capa.nombre`) y `Escape` (`CancelarRenombrar`). La fila de la capa siendo renombrada reemplaza el botón principal del nombre por un `text_input_view` enfocado; el resto de los micro-controles (toggle, slider, blend, mover, dup, elim) sigue habilitado — no se bloquea porque las teclas ya quedan absorbidas por el modal. Vacío al confirmar no pisa el nombre original (un input en blanco rompería la UX de la fila). Tests nuevos (4): F2 emite `IniciarRenombrar`, flow completo `Iniciar → set_text → Confirmar` actualiza el `Capa.nombre`, cancel preserva el original, confirmar con whitespace no pisa.
23. **Undo / redo — stack de snapshots del lienzo** ✓ — `Model.historial: Vec<Lienzo>` + `cursor_historial: usize` mantienen una pila de estados del documento; `historial[0]` es el lienzo inicializado, `historial[cursor]` siempre coincide con `model.lienzo`. Cada mutación reversible (toggle visible, blend ±, opacidad, mover, duplicar, eliminar, agregar local/IA, file drop, picker, renombrar con cambio efectivo) llama `pushear_snapshot` tras `aplicar_y_recomponer` — primero trunca cualquier rama de redo (`historial.truncate(cursor+1)`), después pushea y avanza cursor. Cap `HIST_CAP = 64` estados (~200 KB worst case); cuando se excede descarta del frente y baja el cursor. **Coalesce por etiqueta `(Uuid, &'static str)`**: si la próxima mutación viene con la misma etiqueta que la última Y estamos en el tope, sustituye el snapshot del tope en vez de pushear nueva entrada. Hoy sólo `BumpOpacidad` usa coalesce (`Some((id, "opacidad"))`) para que un drag continuo del slider colapse a una sola entrada reversible — 50 deltas durante un drag = 1 Ctrl+Z, no 50. Hotkeys nuevos: `Ctrl+Z` → `Msg::Undo`, `Ctrl+Shift+Z` y `Ctrl+Y` → `Msg::Redo` (ambos aliasados; ambos globales — no requieren capa seleccionada). `aplicar_undo` / `aplicar_redo` ajustan el cursor y restauran el `Lienzo`; `ajustar_seleccion_tras_restaurar` cae a la última capa visible si la seleccionada ya no existe en ese snapshot (caso típico: eliminé una capa, hago Ctrl+Z, vuelve la capa eliminada y la selección la reapunta). El almacén content-addressed nunca borra buffers, así que cualquier hash referenciado por un snapshot anterior sigue resoluble — `componer` funciona después del Undo sin tocar el almacén. Tests nuevos (10, total 26 verde): hotkey dispatch (Ctrl+Z/Ctrl+Shift+Z/Ctrl+Y), Undo sin historial es no-op + estado descriptivo, Undo revierte toggle (round-trip con Redo), mutación tras Undo trunca redo y deja `nada que rehacer`, 50 BumpOpacidad coalesce a 1 entrada (un Undo deshace el drag entero), drag sobre capa A vs. B no se coalesce entre sí, cap descarta los más viejos cuando `historial.len() > HIST_CAP`, Undo de Eliminar resucita la capa **y** reajusta la selección, confirmar rename con whitespace no genera snapshot, confirmar con nombre distinto sí lo genera y un Undo lo revierte.
24. **Zoom y pan del lienzo** ✓ — el lienzo dejó de ser un `.image()` plano. `Model` gana `factor_zoom: f32` (multiplicador sobre el fit-contain natural; default 1.0, clamp `[ZOOM_MIN=0.05, ZOOM_MAX=32.0]`) y `pan_x`/`pan_y: f32` (offset en px de pantalla desde la posición centrada-fit). `panel_lienzo` reemplaza `.image(img)` por `.paint_with(...)` que aplica `Affine::translate((tx,ty)) * Affine::scale(s)` con `push_layer(Mix::Clip, ...)` sobre el rect del panel — así una imagen zoom-in no sangra a los paneles de capas/ops vecinos. La matemática queda aislada en `transform_lienzo(image_w, image_h, rw, rh, factor_zoom, pan_x, pan_y) -> Option<(s, off_x, off_y)>` — pura, testeable sin gráficos. **Zoom-a-cursor**: `on_wheel` consulta `lienzo_rect_get()` (side-channel `static OnceLock<Mutex<Option<PaintRect>>>` que el painter escribe en cada frame — la única forma que `on_wheel` tiene de saber dónde cayó el cursor sin info de layout en su firma); si el cursor cae dentro del rect del lienzo emite `Msg::Zoom { mult, ancla: Some((rect, cx, cy)) }`. `pan_para_zoom_a_cursor` despeja el nuevo `(pan_x, pan_y)` para que el píxel-imagen bajo el cursor quede fijo (sensación natural del image editor). Wheel fuera del lienzo → `None` (no zoom). Factor por tick: `ZOOM_BASE.powf(-delta.y)` con `ZOOM_BASE = 1.1`; `delta.y > 0 ⇒ zoom out` (convención CSS de `WheelDelta`). **Pan**: `View::draggable(|fase, dx, dy| Move ⇒ Msg::Pan(dx, dy))` sobre el mismo painter; los dx/dy de pantalla se acumulan directo sin reescalar (1 px de drag = 1 px de pan, independiente del zoom). Hotkey nuevo `0` (sin Ctrl) → `Msg::ResetVista` (zoom=1, pan=0,0). Header gana indicador discreto `· vista N%` que sólo aparece cuando `factor_zoom != 1.0` — el caso por defecto no agrega ruido visual. Tests nuevos (11, total 37 verde): `transform_lienzo` cubre fit-contain en zoom=1, aspect distinto con pad simétrico, `factor_zoom=2` descentra correctamente, pan sólo traslada (no afecta escala), dims cero → `None`; `pan_para_zoom_a_cursor` verifica que el píxel bajo cursor permanece fijo bajo un cambio de zoom 1→2 (con cross-check del transform inverso); `dentro_de_rect` borde-inclusive en las 4 esquinas; `Msg::Zoom` clampea a `[ZOOM_MIN, ZOOM_MAX]` en ambas direcciones; `Msg::Pan` acumula offsets; `Msg::ResetVista` y hotkey `0` (sin Ctrl, Ctrl+0 no dispara) restauran defaults.
25. **Herramientas mover/cuentagotas + lectura de color** ✓ — `Model` gana `herramienta: Herramienta` (default `Mover`) y `color_picked: Option<[u8; 4]>`. La herramienta cambia el cableado de eventos del lienzo: `Mover` ⇒ `View::draggable` panea (Fase 24); `Cuentagotas` ⇒ `View::on_click_at` lee el píxel-imagen bajo el cursor. El wheel sigue zoom-eando en ambos modos (la `Msg::Zoom` no depende de la herramienta). `recoger_color_en(image_data, image_w, image_h, lx, ly, rw, rh, factor_zoom, pan_x, pan_y) -> Option<[u8; 4]>` es la función pura que cruza el rect-coords → image-coords vía el `transform_lienzo` de Fase 24 y va al buffer Rgba8 del compuesto (`peniko::Image::data.data()`). El borde se respeta: clicks fuera del rect efectivo de la imagen (caen en el pad del fit-contain o más allá) devuelven `None` y el `update` deja `color_picked` intacto con estado descriptivo. Buffer corto (defensivo) también `None` — el handler nunca panickea por índice fuera de rango. Hotkeys globales: `m` → `CambiarHerramienta(Mover)`, `i` → `CambiarHerramienta(Cuentagotas)` (Photoshop standard). UI: sección "herramienta" arriba del panel ops con dos botones discoverable (`● mover (m)` / `○ cuentagotas (i)`, swap `●`/`○` según activa, `pal_tool_activo` con `bg_selected` resalta la activa). Header agrega `⌨ herramienta` siempre y `🎨 #RRGGBB α=N` cuando hay color leído — el caso por defecto no agrega ruido. Tests nuevos (8, total 45 verde): `recoger_color_pixel_central_a_zoom_1` (verifica 2 píxeles en posiciones distintas con patrón conocido), `recoger_color_fuera_de_imagen` (banda del pad + arriba del rect), `recoger_color_respeta_zoom_y_pan` (zoom 2 → click en esquina del rect cae en píxel-imagen (1,1)), `recoger_color_buffer_corto` (defensivo, no panic), `msg_recoger_color_actualiza_color_picked` (flow completo update con imagen real), `msg_recoger_color_fuera_no_pisa` (estado anterior intacto), `msg_cambiar_herramienta_actualiza_modo` (toggle Mover↔Cuentagotas), `hotkey_m_e_i` (sin/con modificadores).
26. **Capa de relleno sólido** ✓ — cierra el loop pick→use: con el color leído por el cuentagotas, el botón "+ relleno #RRGGBB (W×H)" en el panel ops crea una `Capa::raster` del tamaño del lienzo llena con ese color y la inserta encima de la seleccionada. Sin color leído, fallback `RELLENO_DEFAULT = [128,128,128,255]` (gris medio opaco) — el label dice "+ relleno gris (W×H)" en ese caso. `buffer_relleno(w, h, rgba) -> Vec<u8>` es la función pura (sin alloc-perdida: `Vec::with_capacity(w*h*4)`). `agregar_capa_relleno` reutiliza el mismo contrato que `agregar_capa_desde_archivo`: insertar buffer al almacén content-addressed (dedup automática — dos rellenos del mismo color comparten hash y NO duplican RAM, sólo Uuid distinto), inserción en `idx_sel + 1` (no al tope), `aplicar_y_recomponer`, devuelve `bool` para que el caller decida snapshotear. `Msg::AgregarRelleno` envuelve + pushea snapshot, así que un Undo deshace el relleno. Tests nuevos (6, total 51 verde): `buffer_relleno` tiene tamaño y patrón correctos; default activo cuando `color_picked` es `None` (nombre lleva `#808080`); usa el picked si existe (nombre con el hex correspondiente); dos rellenos del mismo color comparten hash en el almacén pero tienen Uuid distinto (verificación content-addressing); inserción en `idx_sel + 1` con vecina al tope que queda arriba; dispatch del `Msg` crece historial en 1 y un Undo lo revierte.
27. **Combinar capa hacia abajo (merge down)** ✓ — botón `⊕` por fila (a la derecha de `⎘` y antes de `✕`) + hotkey `Ctrl+E` (Photoshop standard, sólo con selección). `combinar_capa_abajo(model, id) -> bool` arma un mini-`Lienzo` con sólo el par (la de abajo primero, la seleccionada arriba — `componer` itera fondo→tope), lo compone con `tullpu_render::componer(&mini, &model.almacen)` honrando blend + opacidad + visible de ambas, mete el Rgba8 al almacén content-addressed, reemplaza el par por una `Capa::raster` nueva con defaults (Normal/1.0/visible — el blend y la opacidad de las originales ya quedan horneados en los píxeles). Nombre `"<abajo> ⊕ <arriba>"` conserva la genealogía. Si la capa ya está al fondo (idx 0) no-op + estado descriptivo "no hay capa debajo". Si `componer` falla (típico: una era derivada stale con `BufferFaltante`), deja el lienzo intacto y reporta. Las hijas derivadas que apuntaban a cualquiera de las dos quedan huérfanas igual que con `Eliminar`. `Msg::Combinar(Uuid)` envuelve + `pushear_snapshot(None)`, así que un Undo restaura el par. Tests nuevos (6, total 57 verde): merge en fondo es no-op con mensaje; dos Normal/opacas se aplanan al color de arriba (la de abajo queda cubierta) y la merged tiene `opacidad=1.0`/`blend=Normal`; opacidad 0.5 da ~promedio ±4 por canal; mismo par mergeado en dos modelos distintos produce el mismo hash (content-addressing); dispatch + Undo restaura ids originales; `Ctrl+E` emite `Combinar` sólo con selección (la `e` suelta no dispara).
28. **Aplanar visibles (merge visible)** ✓ — botón `⊞ aplanar visibles (N) · Ctrl+Shift+E` en sección "estructura" del panel ops + hotkey `Ctrl+Shift+E` (Photoshop standard, global — no requiere selección). `aplanar_capas_visibles(model) -> bool` corre `tullpu_render::componer(&model.lienzo, &model.almacen)` — que ya itera fondo→tope saltando `!visible`, así que el composite ES exactamente "merge visible" — y reemplaza el cluster de capas visibles por una sola `Capa::raster` con defaults (Normal/1.0/visible). **Preservación topológica**: las hidden NO se tocan; el resultado va donde estaba la *más alta* visible. Cálculo: `insert_idx = count(hidden en 0..=max_visible)`. Ej. con `[v0, hid1, v2, hid3]` el merged queda en idx 1 → `[hid1, merged, hid3]` (hid1 sigue debajo, hid3 sigue encima). No-op + estado descriptivo si hay 0 o 1 visibles ("nada visible que aplanar" / "ya hay una sola capa visible"). Hijas derivadas de las merged quedan huérfanas igual que con `Eliminar`/`Combinar`. `Msg::AplanarVisibles` envuelve + snapshot. Tests nuevos (7, total 64 verde): 0 visibles no-op; 1 visible no-op; 2 visibles dan 1 capa con composite correcto y defaults reseteados; hidden intercalado preserva su posición topológica (caso 4→3 capas: bg(v), hidA(h), fg(v), hidB(h) → [hidA, merged, hidB]); hidden arriba de todo termina arriba del merged; dispatch + Undo restaura las 3 originales; `Ctrl+Shift+E` emite `AplanarVisibles` (y `Ctrl+E` sin shift sigue siendo `Combinar` para la capa seleccionada — los dos hotkeys coexisten sin solaparse).
29. **Espejar horizontal / vertical como OpLocal** ✓ — dos variantes nuevas de `OpLocal` (`EspejarHorizontal`, `EspejarVertical`) declaradas en `tullpu-core` e implementadas en `tullpu-ops::aplicar_op_local`. Encajan en el modelo de derivada pixel-a-pixel: el buffer de salida tiene el mismo tamaño que el de entrada, así que se aplican sobre cualquier capa raster o derivada sin tocar las dimensiones del lienzo (la rotación 90° sí cambia dims y queda para otra fase). `espejar_horizontal(src, w, h)` invierte por columna dentro de cada fila; `espejar_vertical` swap-ea filas enteras (más barato — 1 `copy_from_slice` por fila vs. 1 por píxel en el horizontal). Etiquetas `"espejar ↔"` / `"espejar ↕"` en `TransformacionPixel::etiqueta` y en `op_etiqueta` de la app. Botones `+ Espejar ↔` / `+ Espejar ↕` en la sección "locales" del panel ops entre `+ Tonalidad 90°` y `+ Blur radio 4`. Como derivadas honran toda la maquinaria existente: stale tracking si la madre cambia, content-addressing del output, undo/redo, merge_down/aplanar — sin tocar nada. Tests nuevos en `tullpu-ops` (5, total 23 verde): `espejar_horizontal_invierte_columnas` (3×2 con colores por columna verifica el swap en fila 0); doble aplicación es identidad (involución, sin drift); `espejar_vertical_invierte_filas` (2×3); doble aplicación V también identidad; `h∘v == v∘h == rotación 180°` (el píxel central queda fijo y las esquinas opuestas intercambian).
30. **Rotar el lienzo 90° (CW/CCW)** ✓ — operación al *lienzo entero* (no a una capa). Botones `⟳ rotar +90° (CW)` / `⟲ rotar −90° (CCW)` en la sección "estructura" del panel ops. `rotar_buffer_90_cw(src, w, h)` mapea `src(x, y) → dst(h-1-y, x)` con `w_new = h`; `rotar_buffer_90_ccw` es la inversa exacta (`src(x, y) → dst(y, w-1-x)`). Ambas puras. `rotar_lienzo(model, cw) -> bool` orquesta el canvas op: (1) rota cada buffer de capa, inserta el rotado al almacén content-addressed → nuevo hash, actualiza `capa.contenido`; (2) swap `lienzo.width ↔ lienzo.height`; (3) marca TODAS las derivadas como `Frescura::Stale` porque las ops `Espejar↔/↕` (Fase 29) no conmutan con rotación — la cache rotada quedaría incorrecta para esos casos. `aplicar_y_recomponer` regenera las stale en `orden_regeneracion` topológico desde la madre ya rotada. `Msg::RotarLienzo { cw }` + snapshot, así que un Undo restaura dims y hashes originales — el content-addressing del almacén garantiza que CW+CCW vuelve al hash de entrada bit-a-bit (no se duplica el buffer porque el hash ya existía). Tests nuevos (7, total 71 verde): `rotar_buffer_90_cw_mueve_top_left_a_top_right` con 2×3 verifica los 5 mapeos clave (A→top-right, E→top-left, etc.); CCW es inversa exacta de CW; CW×2 = rotación 180° (comparado contra el reverse byte-por-byte); `rotar_lienzo_cw_intercambia_dimensiones` (2×3 → 3×2); CW+CCW restaura dims Y el hash original (content-addressing); lienzo sin capas no-op + estado "nada que rotar"; dispatch + Undo restaura dims.
31. **Auto-trim del lienzo (recortar a visible)** ✓ — botón `✂ recortar a visible (auto-trim)` en la sección "estructura" del panel ops. Photoshop's "Trim Transparent Pixels". `bbox_no_transparente(data, w, h) -> Option<(x0, y0, x1, y1)>` (half-open) escanea el buffer Rgba8 del compuesto buscando el rect más chico que contiene todo lo opaco (alfa > 0). Ignora píxeles con alfa=0 aun con RGB no-cero (PSD a veces deja "pixel data" basura). `recortar_buffer(src, w, x0, y0, x1, y1)` corta filas dentro del rect — sin overhead extra, `extend_from_slice` por fila. `recortar_lienzo_a_visible(model)` espeja `rotar_lienzo` de Fase 30: para cada capa lookup buffer, recorta al rect, mete al almacén content-addressed, actualiza `capa.contenido`; swap dims; marca todas las derivadas `Stale` (Blur y Niveles no conmutan exacto con crop por efectos de borde); `aplicar_y_recomponer` regenera. No-op si el bbox cubre el lienzo entero ("ya está justo, nada que recortar") o si todo el composite es transparente ("lienzo vacío, nada que recortar"). `Msg::AutotrimLienzo` + snapshot, un Undo restaura dims originales. Tests nuevos (9, total 80 verde): bbox de todo-transparente da `None`; bbox de un solo píxel central de 3×3 da `(1,1,2,2)` half-open; bbox del lienzo entero opaco; bbox ignora alfa=0 aun con RGB; `recortar_buffer` extrae subrect 3×2 correcto con verificación pixel-perfect; autotrim no-op si todo opaco ("ya está justo"); no-op si todo transparente ("vacío"); recorte real de 4×4 → 2×2 con isla central + buffer post-crop es del nuevo tamaño correcto; dispatch + Undo restaura dims.
32. **Editar parámetros de derivada con sliders in-situ** ✓ — la sección "parámetros" del panel ops aparece sólo cuando la capa seleccionada es una `OrigenCapa::Derivada` con una `OpLocal` parametrizable (Brillo/Contraste/Saturacion/Tonalidad/Blur/Opacidad — Niveles queda para una fase dedicada con 3 sliders). Cada slider mapea al campo correspondiente con rango fijo: Brillo `[-1, 1]`, Contraste `[0, 3]`, Saturacion `[0, 3]`, Tonalidad `[-180, 180]`, Blur `[0, 20]`, Opacidad `[0, 1]`. El drag emite `Msg::AjustarParametro { id, param, dv }` con `param: ParametroSlider` (enum interno de la app que apunta al campo). `ajustar_parametro_derivada(model, id, param, dv)` hace pattern-matching `(param, op)` para sumar `dv` al campo correcto con clamp; si `param` no concuerda con la variante de `op`, no-op. Tras la mutación: marca la capa `Frescura::Stale` y `lienzo.propagar_stale(id)` para invalidar el cono descendiente. `aplicar_y_recomponer` regenera. Coalesce por `(id, param.clave_coalesce())` — un drag de 50 eventos del slider de Brillo sobre la misma capa colapsa a 1 entrada de historial (mismo patrón que el slider de opacidad de Fase 19). `slider_pal_parametros(theme)` da un slider más ancho que el de la fila (`track_width: 140`, label de 80) porque la sección "parámetros" tiene más espacio horizontal. Tests nuevos (8, total 88 verde): ajustar brillo suma y marca stale; clamp a `[-1, 1]`; `param` no concuerda con `op` es no-op; ajustar sobre raster (no derivada) es no-op; propaga stale al cono (madre → derivada Brillo → derivada Saturacion: ajustar el Brillo invalida la Saturacion); 30 events seguidos coalesce a 1 snapshot y un Undo revierte el drag entero; sliders devuelve None para `Invertir`/`Espejar*`; sliders devuelve Some para las 6 ops parametrizables.
33. **Niveles con 3 sliders interactivos** ✓ — cierra el set de `OpLocal` parametrizables. La sección "parámetros" detecta `OpLocal::Niveles` y muestra 3 rows apilados: `niveles γ` (gamma, clamp `[0.1, 4.0]`), `niveles blanco` (entrada_max, clamp `[0, 1]`), `niveles negro` (entrada_min, clamp `[0, 1]`). Orden visual top→bottom imita el panel Levels de Photoshop. Tres `ParametroSlider` nuevos (`NivelesEntradaMin`, `NivelesEntradaMax`, `NivelesGamma`), cada uno con su `clave_coalesce` (`p:niveles_min`/`max`/`gamma`) — drag de cada slider colapsa por separado, así que ajustar min y luego max produce 2 entradas en el historial (no 1 sola entremezclada). Se permite que `entrada_min > entrada_max`: `aplicar_op_local` protege con `(max - min).max(1e-6)`, y cruzar los niveles es un truco válido (binarización por intervalo invertido). Pattern-matching en `ajustar_parametro_derivada` con los 3 casos nuevos; si el `ParametroSlider` no concuerda con la op (ej. `NivelesEntradaMin` sobre `Brillo`), no-op defensivo. Tests nuevos (6, total 94 verde): los 3 campos mutan en aislamiento dejando los otros intactos; clamps por param (min/max a `[0, 1]`, gamma a `[0.1, 4.0]`); param-no-concuerda no-op; sliders devuelve 3 rows para Niveles; drag de gamma coalesce a 1 snapshot + Undo revierte; drag de min y luego max produce 2 snapshots (coalesce por param distinto).
34. **Histograma RGB del composite** ✓ — sección "histograma" en el panel ops (entre "parámetros" y "entrada") con un chart de 256×72 px que pinta las distribuciones R/G/B del lienzo compuesto. `histograma_rgb(data) -> [[u32; 256]; 3]` es la función pura: cuenta cada valor 0..255 por canal RGB, ignora el alfa (no se incluye como canal ni se usa para ponderar — un píxel con alfa=0 cuenta igual que uno opaco; eso refleja la "tinta presente" en el buffer del compuesto). `Model.histograma: Option<[[u32; 256]; 3]>` cachea el resultado (768 bytes); se recomputa dentro de `aplicar_y_recomponer` después del `componer`, así que el chart siempre está sincronizado con el composite. El painter usa `vello::Scene::fill` para 768 rects (256 bins × 3 canales) con colores translúcidos por canal (R `(220,60,60,180)`, G `(60,200,80,180)`, B `(80,120,230,180)`) — se superponen legiblemente. Normalización por el pico global de los 3 canales para que la mayor barra ocupe el alto entero. Cierra el loop con Fase 33: ahora podés ver la distribución mientras movés los sliders de Niveles en vivo (el cache se refresca en cada `aplicar_y_recomponer`). Tests nuevos (6, total 100 verde): buffer uniforme concentra los 16 píxeles en R[50], G[100], B[200]; el alfa NO afecta el conteo (2 píxeles con alfa 0 y 255 cuentan los dos); gradiente de R 0..255 distribuye 1 por bin; buffer vacío es todo cero; `aplicar_y_recomponer` actualiza el cache (modelo mínimo 4×4 de ceros → 16 en bin 0 de cada canal); agregar un relleno picked refresca el histograma (todos los píxeles caen en los bins del color del relleno).
35. **Selección rectangular básica (marquee)** ✓ — `Herramienta::Marco` (tercera herramienta junto a Mover/Cuentagotas), hotkey `r`. El modo cambia el cableado de eventos del lienzo: `on_click_at` (press) emite `IniciarSeleccion` y `draggable_at` (Move/End) emite `AjustarSeleccion`/`FinalizarSeleccion` — `on_click_at` + `draggable_at` COEXISTEN según `llimphi-ui` (Fase 25 lo descubrió). Estado en `Model`: `seleccion: Option<RectImagen>` (rect commiteado en coords-imagen, half-open `[x0, x1) × [y0, y1)`) + `seleccion_drag: Option<SeleccionDrag>` (estado intermedio mid-drag con ancla, posición local acumulada y `rw, rh` capturados al inicio para que un resize de ventana no descoloque). `local_a_imagen(lx, ly, rw, rh, image_w, image_h, factor_zoom, pan_x, pan_y)` aplica la inversa de `transform_lienzo` (de Fase 24) para convertir click local → coord-imagen. `rect_imagen_desde_drag(...)` normaliza el rect: ordena ancla vs. cursor en cada eje, clampea half-open al `[0, image_w) × [0, image_h)`, devuelve `None` si el área quedó cero (click sin drag). La selección VIVE en coords-imagen — sobrevive zoom/pan; el painter la transforma a coords-pantalla con la misma `transform_lienzo` que la imagen y dibuja **doble-stroke** (3 px negro + 1 px blanco) para "marching ants" estático que se ve sobre cualquier fondo. Hotkey `Esc` global: si hay `seleccion` o `seleccion_drag`, los limpia y emite `LimpiarSeleccion`; sin selección, devuelve `None` (deja que otros modales como picker/rename consuman). La selección NO entra al historial — no es parte del DAG de imagen. Esta fase sólo deposita el state + visual; las ops sobre la selección (crop-a-selección, fill-a-selección, copy) van en fases siguientes. Tests nuevos (11, total 111 verde): `local_a_imagen` zoom 1 centra correctamente y dims cero → None; rect normaliza ancla+cursor invertidos en ambos ejes; clampea al lienzo cuando cur cae fuera; área cero → None; `IniciarSeleccion` setea ancla y limpia selección previa; `AjustarSeleccion` acumula deltas y construye rect en vivo (lienzo 4×4 → rect (0,0)-(4,4)); `FinalizarSeleccion` limpia drag manteniendo rect; `LimpiarSeleccion` borra ambos campos; hotkey `r` emite Marco; hotkey `Esc` emite `LimpiarSeleccion` sólo cuando hay rect activo.
36. **Recortar a la selección** ✓ — primera op sobre el marquee de Fase 35. Botón `✂ recortar a selección (W×H)` en la sección "estructura" del panel ops (etiqueta dinámica: muestra las dims del rect si hay selección, `(—)` si no — discoverable también sin marquee). El núcleo se extrajo a `recortar_lienzo_a(model, x0, y0, x1, y1)` — el mismo helper que ahora alimenta tanto `recortar_lienzo_a_visible` (Fase 31) como `recortar_lienzo_a_seleccion` (esta fase). Espeja `rotar_lienzo`: recorta cada buffer al rect, mete al almacén content-addressed, marca derivadas `Stale`, recompone. La nueva función `recortar_lienzo_a_seleccion(model)` re-clampea contra el lienzo vigente (un rotar/recortar posterior puede haber dejado la selección parcial o fuera), no-op si: no hay selección, intersección con lienzo vacía, o el rect cubre el lienzo entero. Tras el crop **limpia la selección** porque sus coords pertenecen al coord-space anterior — un nuevo drag arma otra. `Msg::RecortarASeleccion` + snapshot, así un Undo restaura dims (la selección NO se restaura — vive afuera del DAG). Tests nuevos (6, total 117 verde): sin selección no-op + estado; selección cubriendo todo no-op; selección fuera del lienzo no-op (clamp deja área 0); recorte real 4×4 → 2×2 central con verificación pixel-perfect post-crop + selección limpia; clampeo parcial cuando el rect sobresale (`(2,2,10,10)` con lienzo 4×4 → 2×2 esquina); dispatch + Undo restaura dims.
37. **Limpiar selección a transparente (Delete)** ✓ — segunda op sobre el marquee. `🗑 limpiar selección (W×H) · Del` en la sección "estructura" (misma política discoverable: botón siempre visible, etiqueta dinámica con dims o `(—)`). El núcleo `limpiar_rect_en_buffer(src, w, x0, y0, x1, y1) -> Vec<u8>` es puro: clona el buffer y pone `[0,0,0,0]` en cada píxel del rect half-open, dejando el resto intacto. `limpiar_seleccion_en_capa(model)` re-clampea la selección contra el lienzo vigente, mete el buffer limpio al almacén content-addressed, repunta `capa.contenido`, propaga stale al cono descendiente y recompone. No-op si: no hay selección, no hay capa seleccionada, **la capa es derivada** (su buffer es cache regenerable — el rechazo manda a "limpiá la raster madre"), el rect queda con área cero tras clampear, o el rect ya era todo transparente (mismo hash post-limpieza, detectado por content-addressing). **La selección se mantiene** tras la op — encaja con el flujo Photoshop "marquee + Delete + re-pintar adentro sin re-armar el rect"; un Esc explícito la limpia. Hotkey: `Delete`/`Backspace` con selección activa → `LimpiarSeleccionEnCapa`; sin selección → `Eliminar` (capa entera, comportamiento previo) — el conflicto se resuelve por el contexto del marquee, no por un modifier. `Msg::LimpiarSeleccionEnCapa` + snapshot, así un Undo restaura el hash original de la capa. Tests nuevos (11, total 128 verde): `limpiar_rect_en_buffer` pixel-perfect (3×2, rect parcial) + rect degenerado no panickea; sin selección / sin capa / fuera del lienzo / sobre derivada / sobre rect ya transparente son no-op con estado descriptivo; limpieza real 4×4 con verificación pixel-perfect de los 4 centrales a cero + 12 de borde intactos + selección preservada; clampeo parcial `(2,2,10,10)` con lienzo 4×4; dispatch + Undo restaura el hash inicial de la capa.
38. **Rellenar selección con el color activo** ✓ — tercera op sobre el marquee, simétrica a la limpieza. `🪣 rellenar selección #RRGGBB (W×H) · ⇧Del` en "estructura" (etiqueta dinámica con el color del cuentagotas — o `gris` del `RELLENO_DEFAULT` — y las dims del rect). El color es `model.color_picked` (leído con el cuentagotas en Fase 25) con fallback a `RELLENO_DEFAULT`. **Refactor**: el cableado común de Fase 37 y 38 se extrajo a `aplicar_a_seleccion_en_capa(model, transformar, verbo, sin_cambio)` — clamp del rect contra el lienzo vigente, rechazo de capas derivadas, no-op por hash content-addressed, propagar stale + recompose; `limpiar_seleccion_en_capa` y `rellenar_seleccion_en_capa` quedan como dos líneas que pasan su `Fn(&[u8], w, x0,y0,x1,y1) -> Vec<u8>` (`limpiar_rect_en_buffer` directo / closure que captura `rgba` sobre `rellenar_rect_en_buffer`). El match inline de etiqueta de color (que estaba duplicado en el botón "+ relleno") se factorizó a `etiqueta_color_activo(Option<[u8;4]>) -> String`. Hotkey: `Shift+Delete`/`Shift+Backspace` con selección → `RellenarSeleccionEnCapa` (la arm va antes de la de limpiar; sin selección cae a `Eliminar`). `Msg::RellenarSeleccionEnCapa` + snapshot reversible. No-op extra si el rect ya tenía ese color exacto (mismo hash). Tests nuevos (10, total 138 verde): `etiqueta_color_activo` hex/gris/ignora-alfa; `rellenar_rect_en_buffer` pixel-perfect + rect degenerado; sin selección / sobre derivada no-op; relleno pixel-perfect con `color_picked` + selección preservada; fallback a gris default sin color; mismo-color no-op; hotkey Shift+Del/Backspace → Rellenar vs. Del pelado → Limpiar; dispatch + Undo restaura el hash inicial.
39. **Duplicar selección a capa nueva (Ctrl+J)** ✓ — cuarta op sobre el marquee, equivalente al "layer via copy" de Photoshop. `⧉ duplicar selección a capa (W×H) · Ctrl+J` en "estructura". Copia los píxeles del rect de la capa seleccionada a una **capa raster nueva** del tamaño del lienzo, transparente fuera del rect, insertada justo encima de la madre y seleccionada. El helper puro `extraer_rect_a_buffer(src, w, h, x0,y0,x1,y1) -> (Vec<u8>, bool)` es el complemento exacto de `limpiar_rect_en_buffer` (aquél conserva el afuera y borra el rect; éste borra el afuera y conserva el rect) — devuelve además si quedó algún píxel con alfa>0 dentro (`false` ⇒ nada visible que copiar). **No es destructivo**: lee `capa.contenido` y no modifica la madre, así que copia desde cualquier capa (raster o derivada — el buffer composite cacheado sirve igual), sin el rechazo de derivadas que sí aplican limpiar/rellenar. No-op si: no hay selección/capa, área cero tras clampear, o el rect era todo transparente. Hotkey `Ctrl+J` sólo con selección activa (sin selección no aplica — la capa entera ya se duplica con Ctrl+D). `Msg::DuplicarSeleccionACapa` + snapshot, así Undo quita la capa nueva. La selección se mantiene. Tests nuevos (8, total 146 verde): `extraer_rect_a_buffer` conserva-dentro/borra-afuera + reporta sin-contenido sobre rect transparente + es complemento byte-a-byte de `limpiar_rect_en_buffer` (suma disjunta reconstruye el original); sin selección / selección transparente no-op sin crear capa; copia real 4×4→capa con sólo el rect 2×2 central, encima de la madre, madre intacta, selección preservada; hotkey Ctrl+J emite sólo con selección; dispatch + snapshot + Undo quita la capa.
40. **Portapapeles interno — copiar/cortar/pegar (Ctrl+C/X/V)** ✓ — quinta op sobre el marquee. `Model.portapapeles: Option<PortaPixeles>` guarda un buffer Rgba8 **recortado al rect** (tamaño `w × h`, no el del lienzo) direccionado por contenido + el origen `(ox, oy)` desde donde se copió. Recortar tight (en vez de un canvas entero como Fase 39) hace que el clip sobreviva a un crop/resize posterior: al pegar se compone sobre el lienzo vigente. **No vive en el DAG** — un undo no toca el portapapeles. Helpers puros: `recortar_subbuffer(src, w, x0,y0,x1,y1) -> (Vec<u8>, bool)` (buffer tight + flag de contenido visible) y `componer_clip_en_canvas(clip, cw, ch, canvas_w, canvas_h, dx, dy) -> Vec<u8>` (blit con recorte por-píxel sobre lienzo fresco transparente). **Copiar** (`copiar_seleccion`, no destructivo, lee `capa.contenido` de cualquier capa, no snapshotea) llena el portapapeles; no-op si el rect era transparente. **Cortar** (`cortar_seleccion`) copia y además llama `limpiar_seleccion_en_capa`; devuelve `true` (⇒ snapshot) sólo si borró — sobre derivada copia pero no borra (estado "copiada, no se borró"). **Pegar** (`pegar_portapapeles`) compone el clip sobre una capa raster nueva en su origen `(ox, oy)` **clampeado** (`ox.min(cw - clip.w)`) para que entre aunque un crop lo haya dejado fuera; inserta encima de la seleccionada y la selecciona. Hotkeys gateados: `Ctrl+C`/`Ctrl+X` exigen selección, `Ctrl+V` exige clip (no selección — crea capa). Tests nuevos (13, total 159 verde): `recortar_subbuffer` tight + reporta contenido/transparente; `componer_clip_en_canvas` ubica en offset + recorta lo que sobresale; copiar llena portapapeles sin tocar historial/capas + extrae origen correcto; copiar transparente no llena; cortar en raster copia+borra (rect transparente, resto opaco); cortar en derivada copia pero no borra (no snapshot); pegar vacío no-op; copy+paste recrea el clip en su origen; pegar clampea origen tras crop que lo dejó fuera; hotkeys gateados por selección/clip; dispatch pegar + snapshot + Undo quita la capa.
41. **Mover los píxeles de la selección — nudge (←↑↓→)** ✓ — sexta op sobre el marquee, primera que reubica contenido. `MoverSeleccion { dx, dy }` (offset con signo en coords-imagen) levanta el contenido del rect de la capa raster, lo borra de su lugar y lo recompone (alpha src-over) en el destino, recortando lo que salga del lienzo. **La selección sigue al contenido** (rect trasladado + clampeado; si queda fuera por completo se limpia). Helper puro nuevo `blit_alpha_sobre(dst, dst_w, dst_h, clip, clip_w, clip_h, dx, dy)` — complemento de `componer_clip_en_canvas` pero **preservando** el contenido previo de `dst` (compone src-over en vez de partir de un lienzo fresco), con offset con signo y recorte de lo que cae fuera; fast-path para alfa 0 (skip) y 255 (copy). `mover_pixeles_seleccion` = `recortar_subbuffer` (levantar) + `limpiar_rect_en_buffer` (borrar origen) + `blit_alpha_sobre` (depositar). No-op si: no hay selección/capa, capa derivada, área cero, o el buffer no cambia (delta cero o todo fuera, detectado por hash). Hotkeys: flechas nudgean 1 px, `Shift` 10 px (`paso_nudge`), sólo con selección activa. Fila de botones `◀▲▼▶` co-locada en el panel ("mover selección · ←↑↓→"). **Snapshots coalescen** por capa (etiqueta `(id, "mover_sel")`): una ráfaga de flechas = un solo Undo, igual que el drag de opacidad. Tests nuevos (11, total 170 verde): `paso_nudge` shift/sin-shift; `blit_alpha_sobre` opaco pisa / transparente no borra / semitransparente compone (gris medio) / recorta offset negativo; mover sin selección / sobre derivada / delta cero son no-op; traslado real deja transparencia en origen + aterriza en destino + selección sigue; movimiento parcialmente fuera recorta y clampea la selección; hotkeys de flecha emiten signo/paso correctos y gatean por selección; tres nudges coalescen a una entrada + Undo restaura el hash original.
42. **Arrastrar el contenido de la selección con el mouse (drag-to-move)** ✓ — la versión pulida del nudge de Fase 41. Con la herramienta Marco, un press que cae **dentro** de la selección vigente arrastra su contenido en vez de armar un marquee nuevo (Photoshop standard); un press afuera sigue armando marquee. Estado nuevo `Model.mover_drag: Option<MoverDrag>` (excluye mutuamente a `seleccion_drag`): guarda la posición local del press, la actual (acumulando los `dx, dy` de cada Move) y el offset entero en coords-imagen ya aplicado (`aplicado_*`). El handler de `IniciarSeleccion` decide la rama convirtiendo el press a coord-imagen (`local_a_imagen`) y testeando contención en el rect. El de `AjustarSeleccion`, si hay `mover_drag`, convierte el desplazamiento local acumulado a coords-imagen dividiendo por la escala `s` del fit (`transform_lienzo`), y mueve el contenido por el **paso entero** que todavía falte aplicar (`mover_pixeles_seleccion`), guardando el resto sub-píxel implícito en `cur - press` — así un drag lento de < 1 px no salta, y uno rápido aplica varios px de una. `FinalizarSeleccion` cierra el drag dejando la selección donde quedó. Snapshots coalescen con la misma etiqueta `"mover_sel"` que el nudge → todo el drag = un solo Undo. Tests nuevos (4, total 174 verde): press dentro arranca `mover_drag` sin limpiar la selección; press afuera arranca marquee y limpia; drag +2px traslada contenido + transparencia en origen + selección sigue + finalizar limpia el drag; sub-píxel (0.4) no mueve hasta acumular > 0.5.
43. **Gestión de la selección — seleccionar todo (Ctrl+A) + expandir/contraer** ✓ — operaciones sobre el rect mismo (no sobre píxeles), fuera del DAG. `SeleccionarTodo` arma un marquee que cubre `(0,0)..(w,h)` y limpia cualquier drag en curso (hotkey `Ctrl+A`, global). `ExpandirSeleccion(delta)` crece (`delta > 0`) o contrae (`delta < 0`) el rect `delta` px por lado vía el helper puro `expandir_rect(rect, delta, w, h) -> Option<RectImagen>` (clampea al lienzo; devuelve `None` si la contracción colapsa el área → la selección se limpia). Ninguna toca el historial. UI: sección "selección" en el panel con botón `▣ seleccionar todo (W×H) · Ctrl+A` + fila de mini-botones `⊖ −1 / ⊕ +1 / ⊕ +10 / ✕ sel`. Tests nuevos (6, total 180 verde): `expandir_rect` crece+clampea / contrae / colapsa a None; seleccionar-todo cubre el lienzo y limpia drags; hotkey Ctrl+A; expandir dispatcha sin tocar historial + contraer de más colapsa; expandir sin selección no-op.
44. **Balde de pintura — flood fill (`g`)** ✓ — primera herramienta que pinta píxeles directamente (hasta acá todo eran ops de capa). Nueva `Herramienta::Balde`; un click hace flood fill desde el píxel clickeado con el color activo (cuentagotas o gris default) sobre la capa raster seleccionada, acotado a `model.seleccion` si la hay. Helper puro `flood_fill(src, w, h, sx, sy, nuevo, tol, bounds) -> Option<Vec<u8>>`: expande en **4-conexión** desde la semilla a todos los píxeles cuyo color esté dentro de `tol` (suma de |Δ| RGBA, métrica 0..=1020) respecto al color semilla, con stack explícito + bitmap de visitados; `bounds` half-open confina el relleno (sus bordes son muro) y una semilla fuera no rellena; la tolerancia se chequea SIEMPRE contra el color original (se pinta sólo al visitar, así que un vecino nunca se evalúa con un color ya modificado); devuelve `None` si nada cambió. `TOL_BALDE = 32` (≈8/canal, tolera leve antialias). `rellenar_flood_en_capa` rechaza capas derivadas y snapshotea. Click→imagen vía `local_a_imagen` (misma inversa que el marquee). Hotkey `g`; botón de herramienta `○/● balde (g)`. Tests nuevos (8, total 188 verde): rellena región contigua respetando borde de color; 4-conexión no cruza diagonal; confinado a bounds; semilla fuera de bounds → None; mismo color → None; tolerancia agrupa cercanos y excluye lejanos; `rellenar_flood_en_capa` pinta raster + rechaza derivada; hotkey `g`.
45. **Pincel a mano alzada (`p`)** ✓ — segunda herramienta de pintura directa, drag-based. Nueva `Herramienta::Pincel`; un drag pinta un trazo continuo con el color activo sobre la capa raster seleccionada (acotado a `model.seleccion` si la hay). Estado `Model.pincel_drag: Option<PincelDrag>` acumula la posición local y guarda el último punto pintado en coords-imagen (`last_i*`) para interpolar el segmento al punto nuevo y no dejar huecos en movimientos rápidos. Helpers puros: `estampar_disco(buf, w, h, cx, cy, radio, color, bounds)` (disco lleno `dx²+dy²≤r²`, reemplazo directo, recorta a canvas+bounds) y `trazar_linea_pincel(...)` (estampa discos a cada paso entero del eje más largo del segmento → continuo para `radio≥1`). `RADIO_PINCEL = 3`. El cableado común vive en `pincel_aplicar(model, dibujar)` (valida raster, resuelve color+bounds, clona buffer, aplica el closure, repunta+stale+recompose si cambió el hash); `pincel_punto_en_capa` (inicio) y `pincel_segmento_en_capa` (continuación) lo usan. Handlers: `IniciarTrazo` (press, local→imagen, estampa disco + setea `pincel_drag`), `ContinuarTrazo` (Move, acumula local, pinta segmento desde `last_i` al nuevo, avanza `last_i` aunque el segmento no cambie), `FinalizarTrazo` (cierra el drag y **resetea `ultima_etiqueta_snapshot`** para que cada trazo sea un Undo independiente). Snapshots coalescen con etiqueta `"pincel"` → un trazo entero = un Undo. Hotkey `p` + botón de herramienta. Tests nuevos (6, total 194 verde): `estampar_disco` pinta el círculo respetando radio (cruz, no esquinas) + recorta a canvas/bounds; `trazar_linea_pincel` continua sin huecos; `pincel_punto_en_capa` pinta raster + rechaza derivada; trazo completo coalesce a 1 entrada y `FinalizarTrazo` corta la cadena (segundo trazo = entrada nueva, Undo selectivo); hotkey `p`.
46. **Pincel pro — radio ajustable + borrador + trazo con alpha** ✓ — tres mejoras al pincel que comparten el estampado. (a) **Radio ajustable**: `Model.radio_pincel` (default `RADIO_PINCEL=3`, tope `RADIO_PINCEL_MAX=64`); `Msg::BumpRadioPincel(±1)` clampea; `[`/`]` ahora son **contextuales** (radio si la herramienta es de trazo — `Herramienta::es_trazo()` —, opacidad de capa si no); fila de control `−/+ ⌀ N px (r=…)` visible sólo con pincel/borrador activo. (b) **Borrador**: `Herramienta::Borrador` (hotkey `e`) reusa todo el cableado de trazo (`IniciarTrazo`/`ContinuarTrazo`/`FinalizarTrazo`); el handler pasa `borrar = herramienta == Borrador` y `estampar_disco` pone `[0,0,0,0]` en vez de pintar. (c) **Trazo con alpha**: el pincel pasó de reemplazo directo a **componer src-over** — el núcleo se extrajo a `mezclar_src_over(dst, src)` (fast-path alfa 0/255), compartido con `blit_alpha_sobre` (Fase 41, refactorizado para usarlo); un color opaco pisa como antes, uno con alfa<255 deja trazo translúcido. `estampar_disco`/`trazar_linea_pincel`/`pincel_{punto,segmento}_en_capa` ganaron el flag `borrar`. Tests nuevos (7, total 201 verde): `mezclar_src_over` opaco/transparente/semi; `estampar_disco` borrar pone alfa cero + color semitransparente compone; `BumpRadioPincel` clampea a `[0,MAX]`; hotkey `e` → borrador; `[`/`]` contextual (radio con pincel/borrador, opacidad con mover); borrador vía trazo borra píxeles. (Se actualizó el test de Fase 27 `hotkey_ctrl_e`: la `e` suelta ahora cambia a borrador, antes era no-op.)
47. **Dureza / suavidad del pincel (soft brush)** ✓ — `Model.dureza_pincel` en `[0,1]` (default `DUREZA_PINCEL=1.0` = borde duro). Helper puro `cobertura_pincel(d, r, dureza) -> f32`: dentro del núcleo `dureza·r` la cobertura es 1.0; entre ahí y `r` cae linealmente a 0; fuera de `r` es 0 (radio 0 o dureza 1 → binario). `estampar_disco` usa la distancia real `√(dx²+dy²)` para la cobertura: al pintar, escala el alfa del color por la cobertura y compone src-over (borde translúcido); al borrar, baja el alfa destino por la cobertura (`a·(1−cob)`) — **goma suave que sólo toca el alfa, preservando el RGB** (más correcto: el RGB bajo transparencia total es irrelevante). `dureza` se propagó por `trazar_linea_pincel` y los orquestadores. Ajuste: `{`/`}` (Shift+`[`/`]`) ∓10% sólo con herramienta de trazo; fila `−/+ dureza N%` en el panel. Tests nuevos (5, total 206 verde): `cobertura_pincel` dura binaria + suave degrada lineal (dureza 0 y 0.5); estampado suave deja centro opaco/borde translúcido; `BumpDurezaPincel` clampea `[0,1]`; `{`/`}` ajustan dureza sólo con trazo; goma suave baja alfa parcial (centro ~0, borde ~intacto). (Se ajustaron los tests de borrador de Fase 46: la goma ahora deja alfa 0 con RGB intacto, antes ponía `[0,0,0,0]`.)
48. **Trazo en línea recta con Shift** ✓ — Shift+click con pincel/borrador traza una recta desde el último punto pintado hasta el click (Photoshop standard). Problema de plumbing: el handler de click de `llimphi-ui` (`on_click_at`) NO recibe modifiers; en vez de extender el hot-path del framework (compartido, riesgoso), se trackea el estado vivo de Shift sólo en tullpu: `on_key` intercepta `Key::Named(NamedKey::Shift)` (ambas Shift llegan así) y emite `Msg::SetShift(pressed)`, que actualiza `Model.shift_held`. `Model.ultimo_pincel: Option<(i32,i32)>` guarda el último punto pintado **persistente entre trazos** (a diferencia de `pincel_drag.last_i*`, que muere con el drag); lo actualizan tanto `IniciarTrazo` como `ContinuarTrazo`. En `IniciarTrazo`, si `shift_held && ultimo_pincel.is_some()` se pinta un segmento (`pincel_segmento_en_capa`) en vez de una pincelada puntual. Tests nuevos (4, total 210 verde): `on_key` con Shift press/release emite `SetShift(true/false)`; `SetShift` actualiza el estado; Shift+click traza la fila completa entre dos puntos (sin huecos); sin Shift los dos clicks quedan como puntos sueltos (el medio sin pintar).
49. **Simetría de trazo (mirror painting)** ✓ — `Model.simetria: Simetria { Ninguna, Vertical, Horizontal, Ambas }` refleja cada estampa del pincel/borrador sobre el/los eje(s) central(es) del lienzo (Vertical = izq↔der, Horizontal = arr↔ab, Ambas = 4 estampas). Helpers puros: `ejes_simetria(sim) -> Vec<(bool,bool)>` (flags flip por eje, siempre incluye la original) y `aplicar_eje(x, y, w, h, flip) -> (i32,i32)` (espejo sobre el centro, `w-1-x`). Los orquestadores `pincel_{punto,segmento}_en_capa` reciben `sim` y, dentro de la misma mutación de buffer (un solo snapshot), estampan en cada eje — para segmentos reflejan ambos extremos con el mismo eje. Cicla con `s` (`Simetria::siguiente`) o el botón `simetría ✕/↔/↕/✛` (en el bloque de controles de trazo). Tests nuevos (6, total 216 verde): `ejes_simetria` cuenta correcta (1/2/2/4); `aplicar_eje` refleja sobre el centro en las 4 combinaciones; `siguiente` cicla; hotkey `s` + handler; pincel con simetría vertical pinta ambos lados (y no el eje Y); simetría Ambas pinta los 4 cuadrantes.
50. **Herramienta degradé (linear gradient)** ✓ — `Herramienta::Degradado` (hotkey `d`): un drag define un eje y al soltar rellena un degradé lineal del color activo (en el ancla, `t=0`) a transparente (en el extremo, `t=1`), compuesto src-over sobre la capa raster, acotado a la selección. Estado `GradienteDrag` (ancla en coords-imagen + posición local acumulada), con `IniciarDegradado`/`AjustarDegradado`/`FinalizarDegradado` (mismo patrón on_click_at+draggable_at que el marquee). Helper puro `rellenar_gradiente(src, w, h, ax,ay, bx,by, color, bounds) -> Vec<u8>`: proyecta el centro de cada píxel sobre el eje (`t = ((p−a)·(b−a))/|b−a|²` clamp `[0,1]`), escala el alfa por `(1−t)` y compone src-over; eje de longitud cero → relleno sólido. `rellenar_gradiente_en_capa` reusa `pincel_aplicar` (validación raster + color + bounds). Botón `○/● degradé (d)`. Tests nuevos (5, total 221 verde): fade horizontal de opaco a transparente (alfa decreciente, 0 en el extremo); eje cero = relleno sólido; respeta bounds (fuera intacto); hotkey `d`; drag completo rellena la capa + cierra el drag + snapshot.
51. **Curvas tonales con editor interactivo** ✓ — la generalización de Niveles (33) + Histograma (34): `OpLocal::Curvas { puntos: Vec<(f32,f32)> }` es una función de transferencia `entrada→salida` definida por puntos de control en `[0,1]²`, aplicada por igual a los tres canales RGB (alfa intacto). `tullpu_ops::lut_curva(puntos) -> [u8;256]` ordena/clampa/deduplica los puntos y los interpola con **Hermite cúbica de tangentes monótonas (Fritsch–Carlson)** — sin el overshoot de Catmull-Rom (una S de contraste no rebota fuera de `[0,1]` ni invierte su pendiente); fuera del dominio cubierto aplana al extremo (clamp, como Photoshop); < 2 puntos válidos → identidad. `aplicar_op_local` mapea la LUT por canal vía `mapear_rgb`. `OpLocal::curvas_identidad()` siembra la diagonal `(0,0)→(1,1)`. **Editor interactivo** en la sección "curva" del panel: canvas `paint_with` (170 px) que pinta el histograma de luminancia tenue de fondo, grilla + diagonal identidad de referencia, la curva (polilínea de la LUT) y los puntos de control como cuadritos (el activo resaltado en ámbar). Click engancha el punto más cercano dentro de un umbral (~6 % del lado) o **inserta uno nuevo** si cae lejos (`CurvaPress` vía `on_click_at`); el drag lo mueve (`CurvaArrastrar`/`CurvaSoltar` vía `draggable_at`) — los extremos sólo en `y` (x fijo en 0/1), los interiores acotados en `x` entre vecinos para no cruzarlos. `Model.curva_arrastrando: Option<CurvaDrag{idx,rw,rh}>` guarda el punto activo + dims del canvas para normalizar los deltas-px a `[0,1]`. Botón `↺ reset curva` y `+ Curvas` en "locales". Snapshot diferido al `End` (1 entrada de historial por gesto, coalesce `"curva"`). Tests: 7 en `tullpu-ops` (identidad, < 2 puntos, extremos+medio levantado, monotonía S-curve dentro de rango, clamp fuera de dominio, orden indiferente, inversión); 7 en la app (insert lejano + engancha idx, engancha cercano sin insertar, drag mueve y, extremo fija x=0, drag sin press no-op, reset, no-op sobre capa no-curva). Total app 228 verde.

52. **Máscaras de capa editables** ✓ — el modelo (`Capa::mascara: Option<Hash>`) y el compositor (`tullpu-render` multiplica el alfa por la máscara de un canal) ya soportaban máscaras desde el día 1, pero la app nunca las exponía. Esta fase cierra el loop no destructivo: una **máscara** es un buffer de un canal (W·H bytes, `255`=visible / `0`=oculto) adjunto a la capa, aparte de su contenido. Sección "máscara" en el panel ops con 5 ops (etiquetas dinámicas según la capa tenga o no máscara): `🎭 + máscara (blanca)` (`agregar_mascara` — todo 255, no-op si ya tiene), `🎭 máscara desde selección` (`agregar_mascara_de_seleccion` — 255 dentro del marquee, 0 fuera; la vía no destructiva equivalente a "recortar a selección" sin perder lo de afuera), `🔄 invertir máscara` (255↔0), `✕ quitar máscara` (vuelve a `None`, no destructivo), `⬇ aplicar máscara al alfa` (`aplicar_mascara` — hornea `alfa·mascara/255` al raster y la quita; sólo raster, las derivadas son cache). Marca `🎭` antes del nombre de capa en el panel cuando hay máscara. **Coherencia geométrica:** `recortar_buffer`/`rotar_buffer_90_{cw,ccw}` se generalizaron con variantes `_bpp` (4=Rgba8, 1=máscara), y `recortar_lienzo_a`/`rotar_lienzo` ahora transforman la máscara junto al contenido — sin esto un crop/rotación dejaría la máscara con dims viejas y el render fallaría por tamaño. Todas las ops snapshotean (1 Undo por gesto). Tests: 9 en la app (set+snapshot, idempotencia, visible-dentro/oculto-fuera, invertir, quitar no destructivo, hornear alfa, rechazo en derivada, crop preserva máscara válida, rotación preserva máscara válida). Total app 237 verde.
53. **Pintar sobre la máscara** ✓ — `Model.editando_mascara: bool` (toggle `✎ editando: contenido/máscara 🎭` en la sección "máscara", resaltado cuando activo). Cuando el modo está activo **y** la capa tiene máscara (`pintando_en_mascara`), las cuatro herramientas de trazo (pincel/borrador/balde/degradé) escriben el buffer de máscara de un canal en vez del contenido Rgba8; si la capa no tiene máscara, el trazo cae al contenido como siempre (flag puro de UI, persiste al cambiar de capa). Semántica value-lerp por cobertura, sin color ni src-over: **pincel revela (255), borrador oculta (0)**, balde rellena la región contigua a 255, degradé revela desvaneciéndose hacia el extremo (calco del degradé Rgba8 que se desvanece a transparente). Implementado con un set paralelo de estampadores de 1 canal (`estampar_disco_mascara`, `trazar_linea_mascara`, `flood_fill_mascara`, `rellenar_gradiente_mascara`) y `mascara_aplicar` (espejo de `pincel_aplicar`: resuelve el buffer de máscara, repunta `capa.mascara`, recompone **sin** propagar stale — la máscara no entra en el cómputo de las derivadas, sólo en el composite). Por eso pintar la máscara funciona también sobre capas derivadas (a diferencia del trazo en contenido, que exige raster). Los cuatro entry-points (`pincel_punto_en_capa`/`pincel_segmento_en_capa`/`rellenar_flood_en_capa`/`rellenar_gradiente_en_capa`) bifurcan al inicio. Reusa los gestos/coalesce/Undo existentes (el snapshot guarda el `Lienzo` con el hash de máscara). Tests: 6 nuevos (gate de modo+máscara, pincel pinta máscara no contenido, borrador oculta, balde rellena a 255, flood 1-canal, estampa lerp). Total app 243 verde. Pendiente fase 54: thumbnail de la máscara en la fila de capa + pintar con valor de gris arbitrario (hoy sólo 0/255).

Pendiente del SDD: export a PSD (post-MVP — el crate `psd` upstream sólo lee; un writer puro-Rust sería un esfuerzo dedicado), máscaras de capa PSD (bloqueado upstream — `psd` no expone máscaras todavía). Mejoras opcionales no críticas: proveedor ONNX real en `pixel-verbo-daemon` (segment-anything / upscale / restyle reales en vez del mock determinista), tiling para imágenes > ~32 MP (refactor de `tullpu-core` y `tullpu-render` para que una capa sea un grafo de tiles content-addressed — habilita dedup por tile sobre fondos repetidos).

**Estimación gruesa** (de `PLAN.md` §6.ter): tullpu base 3-4 meses · foreign-psd 2 sem post-tullpu.

## Relación con otros dominios

- **[[pluma]]** — el haz de cuerpos es el patrón madre/hija/stale que tullpu calca sobre píxeles.
- **[[pineal]]** — `pineal-export` para la salida; `pineal` es visualización de datos, tullpu es edición de imagen (no se solapan).
- **[[nahual]]** — `nahual-image-viewer-llimphi` es *viewer* (solo lectura); tullpu es *editor*. nahual decodifica PNG/JPEG con el crate `image`, mismo punto de entrada que tullpu para raster.
- **[[llimphi]]** — `llimphi-widget-nodegraph` (grafo de capas/ops) + `llimphi-surface` (lienzo vivo, primitivo nuevo del motor).
