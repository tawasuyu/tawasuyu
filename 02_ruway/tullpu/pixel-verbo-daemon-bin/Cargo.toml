[package]
name = "pixel-verbo-daemon-bin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true
description = "pixel-verbo — binario del daemon. Levanta un Proveedor de píxel en RAM y lo sirve a N procesos por socket Unix. Hoy: proveedor mock determinista. Mañana: ONNX (segment-anything, upscale, restyle)."

[[bin]]
name = "pixel-verbo-daemon"
path = "src/main.rs"

[dependencies]
pixel-verbo-core = { path = "../pixel-verbo-core" }
pixel-verbo-daemon = { path = "../pixel-verbo-daemon" }
pixel-verbo-mock = { path = "../pixel-verbo-mock" }
clap = { workspace = true }
anyhow = { workspace = true }
