[package]
name = "tullpu-ops"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true
description = "tullpu — catálogo de operaciones locales (deterministas en proceso) + orquestador `regenerar_stale` que recorre el cono derivado del lienzo."

[dependencies]
tullpu-core = { path = "../tullpu-core" }
tullpu-render = { path = "../tullpu-render" }
pixel-verbo-core = { path = "../pixel-verbo-core" }
image = { workspace = true }
thiserror = { workspace = true }
postcard = { workspace = true }
tiny-skia = { workspace = true }

[dev-dependencies]
pixel-verbo-mock = { path = "../pixel-verbo-mock" }
