# shuma-link

> Clickable output links of [shuma](../../README.md).

Detects URLs, absolute paths, `file:line:col` and makes them clickable. Click opens the browser / editor by type.

## Deps

- [`shuma-core`](../shuma-core/README.md), `regex`
