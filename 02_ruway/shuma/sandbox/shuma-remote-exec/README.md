# shuma-remote-exec

> Gateway-mediated remote exec of [shuma](../../README.md).

Runs a command on a remote host with stdout/stderr streamed live. No pty (it's an exec, not a shell).

## Deps

- [`shuma-protocol`](../shuma-protocol/README.md)
