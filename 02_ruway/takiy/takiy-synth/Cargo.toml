[package]
name = "takiy-synth"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true
description = "takiy — síntesis: render de un Score a audio PCM. Osciladores básicos + ADSR; escritor WAV minimal. Capa Renderer pluggable para que SoundFonts/IA reemplacen el oscilador sin tocar el resto."

[dependencies]
takiy-core = { path = "../takiy-core" }

[dev-dependencies]
serde_json = { workspace = true }
