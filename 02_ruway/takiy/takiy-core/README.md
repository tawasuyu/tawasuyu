# takiy-core

> Musical model of [takiy](../README.md): note, sequence, voice.

`Note { pitch, vel, dur }`, `Sequence`, `Voice`. No audio output — only structure. `Voice` assigned to a synth at runtime.

## Deps

- `serde`
