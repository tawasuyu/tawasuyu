[package]
name = "card-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true
description = "Brahman — Tarjeta de Presentación canónica (identidad arje + flujos tipados brahman)."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
ulid = { workspace = true }
# Atestación al arranque: la Seed Card lleva ConcesionCapacidad firmadas
# (mismo tipo que agora/wawa verifican) sobre los binarios críticos.
format = { path = "../../format" }

[dev-dependencies]
postcard = { workspace = true }
