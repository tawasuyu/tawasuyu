[package]
name = "sandokan-monitor-core"
description = "Cara de sólo-lectura del plano de control: observa un Engine (list+status+telemetry) y produce un MonitorSnapshot. El process monitor consume ESTO, no /proc ni el card store crudo — una sola fuente de verdad (ver shared/sandokan/SDD.md §6)."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
sandokan-core = { path = "../sandokan-core" }
sandokan-lifecycle = { path = "../sandokan-lifecycle" }
ulid = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
async-trait = { workspace = true }
tokio = { workspace = true }
