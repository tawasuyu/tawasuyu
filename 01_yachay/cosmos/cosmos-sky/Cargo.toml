[package]
name = "eternal-sky"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Ergonomic public façade over eternal-* astronomy crates: civil time → planetary positions → apparent topocentric sky."
# Kept unpublishable for now because it transitively depends on
# eternal-validation (publish = false). The plan is to migrate the core
# engine modules (oracle, topocentric, sidereal, delta_t) here when
# preparing the v1.0 release and then flip this back on.
publish = false

[dependencies]
eternal-core.workspace = true
eternal-time.workspace = true
eternal-coords.workspace = true
eternal-ephemeris.workspace = true
eternal-validation = { path = "../cosmos-validation" }
libm.workspace = true
thiserror.workspace = true

[dev-dependencies]
approx.workspace = true
